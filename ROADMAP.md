# KM27 — Implementation Roadmap

The single source of truth for *what's built, what's in flight, and what's queued*.

## How to use this file

- **Phases** are vertical slices. Each one ends in something that runs and can be playtested.
- **Tick the `- [ ]` boxes** as deliverables land. Don't skip phases; each builds on the previous.
- **Update the Progress Log** at the bottom every session that ships code — newest entry first, ISO date prefix.
- **GDD refs** point to sections of `GDD.md`. If a phase contradicts the GDD, the GDD wins — update the GDD first, then the roadmap.
- **Tuning numbers** (enemy power, yields, etc.) are placeholders per the GDD. Don't re-balance until Phase 8 unless something is unplayable.

---

## Phase 0 — Godot scaffold

**Goal:** A Godot 4 project that opens, runs `Main.tscn`, and proves the autoloads are wired.

**Deliverables**
- [x] `project.godot` with name, main scene, autoloads, 1280×720 viewport, GL Compatibility renderer.
- [x] `icon.svg` placeholder ("KM" mark).
- [x] `scenes/Main.tscn` — minimal Control scene with a centred "booting…" label.
- [x] `scripts/main.gd` — prints week/year + starting resources on `_ready()`.
- [x] `scripts/autoload/game_state.gd` — stub with week, year, phase, resources, roster, world, tournament_streak.
- [x] `scripts/autoload/event_bus.gd` — empty signal hub with planned-signal comments.
- [x] `scripts/autoload/rng.gd` — seedable `RandomNumberGenerator` wrapper.
- [x] Folder layout (`scenes/`, `scripts/`, `scripts/autoload/`, `assets/textures/`, `assets/audio/`, `data/`).

**Done when:** opening `project.godot` in Godot 4.3+ loads without errors and the main scene prints `[KM27] Main scene ready. Year 1627, Week 1.` followed by the resource dictionary.

---

## Phase 1 — Data layer & World generation *(GDD §3, §4)*

**Goal:** Generate a deterministic 15×15 world that matches GDD §4 exactly, with no UI yet.

**Deliverables**
- [x] `Unit` class (`scripts/data/unit.gd`) — id, name, class (Squire/Knight), visible stats, hidden PA, current task/expedition state.
- [x] `Stats` resource — Strength, Speed, Technique, Bravery, Loyalty, Determination, Swordsmanship, Archery, Horsemanship, Leadership, Etiquette, Intimidation. Helpers for sum, clamp-on-set, PA-aware increment.
- [x] `ResourceBundle` helper — add/subtract dictionaries for wood/fibres/copper_ore.
- [x] `MapTile` class — coords, terrain, knowledge state, optional castle ref; resource type is derived from terrain.
- [x] `Castle` class — coords, difficulty (30–200 spread with ±10 jitter), pre-rolled reward bundle.
- [x] `World` class — 15×15 grid, town at (7,7), 9 starting Explored tiles, 8 castles placed with the "no castle within 2 tiles of town" rule (Chebyshev ≥ 3).
- [x] `WorldGenerator.generate(seed: int) -> World` static helper using `RNG.seed_run()`.
- [x] Debug scene `scenes/dev/world_dump.tscn` that prints the grid + castle list to the output panel and runs a battery of validation checks + a determinism re-roll.

**Done when:** running the debug scene with the same seed twice produces an identical world, castle difficulties span 30–200, and no castle sits within 2 tiles of (7,7).

**Notes**
- `Tile` was renamed `MapTile` to dodge any future clash with Godot built-ins.
- Terrain is uniformly random across 7 wilderness types (Village/Plains/Forest/Hills/Mountain/Beach/Ocean); per GDD §4 "MVP uses pure random distribution — tuning the placement formula is future work."
- Run the dev scene in Godot with **F6** while `scenes/dev/world_dump.tscn` is open.

---

## Phase 2 — Game state & weekly clock *(GDD §5, §6, §16)*

**Goal:** A headless week-advancer that loops Plan → Tick → Pre-Battle Review → Resolution and rolls events correctly.

**Deliverables**
- [x] `GameState` populated with `world`, `roster: Array[Unit]`, `resources: ResourceBundle`, `tournament_streak`, `current_event`, plus `current_year()` / `current_week_of_year()` / `is_tournament_week()` helpers and `start_run(seed)` / `advance_to_next_week()` / `roll_current_event()` lifecycle methods.
- [x] `PhaseMachine` (held on GameState, not a separate autoload) with explicit `Phase` enum, `transition()` / `advance()` and a `phase_changed` signal routed through EventBus.
- [x] `EventRoller` — uniform pick across Away / Home / Battle Event on normal weeks; Tournament on week-12N; Grand Tournament substitution when `tournament_streak >= 2`.
- [x] `Calendar` helpers (`year_for`, `week_of_year`, `is_tournament_week`, `tournament_number`).
- [x] `EventKind` constants + label helper.
- [x] Headless test scene `scenes/dev/event_roll_test.tscn` (F6) — runs 50 weeks with a pinned seed, simulates winning every tournament, tallies, and validates the override + Grand-substitution rules.

**Done when:** 50-week dry run produces ~33% each event type outside tournament weeks, a Tournament on every week-12N, and a Grand Tournament after winning 2 in a row.

**Notes**
- `EventBus` now declares the full set of signals we'll need across phases 2–7 (commented at the top of `event_bus.gd`).
- `GameState.roll_current_event()` is the single entry point for Planning to ask "what's happening this week?" — it stores on `current_event` and emits `event_rolled`.

---

## Phase 3 — Starting flow & Roster UI *(GDD §3, §9, §10)*

**Goal:** Player can start a run, pick a Knight, and see their 4-unit roster with correct stat ranges.

**Deliverables**
- [x] Title screen (`scenes/Main.tscn`) — randomised seed display + "Begin a New Run" button that hands off to the Knight chooser.
- [x] `scenes/screens/knight_chooser.tscn` — 3 randomly-rolled Knight candidates (stats 7–14 + flat +1, PA 100–180); each card has its own "Take into service" button.
- [x] Squire stat roller (4–10, PA 60–140, no bonus) inside `RosterGenerator.build_starting_roster()`.
- [x] `scenes/screens/roster_view.tscn` — 4 unit cards with visible stats, status line, and stat-total readout. PA hidden everywhere.
- [x] `scripts/systems/determination.gd` — `should_trigger(week)` + `roll_for_units(units)` honouring the PA cap and skipping expedition units. (Phase 5's Tick wires the call.)
- [x] Shared `UnitCard.build(unit, on_choose?, label?)` builder so the same card renders identically across chooser / roster / later screens.
- [x] `NamePool.random_name()` (32 first names × 25 surnames) for procedural Knight/Squire names.

**Done when:** new run produces 1 Knight + 3 Squires inside the documented stat ranges, the chosen Knight has the class bonus, and the roster view renders correctly.

**Notes**
- "Small flat bonus" for the Knight class (GDD §9) was implemented as `+1` to every visible stat, clamped at 20. Tunable via `RosterGenerator.KNIGHT_FLAT_BONUS`.
- The title screen seeds the run via `randi()`. World gen, Knight rolls, and Squire rolls all consume the same seeded `RNG` autoload so the whole starting roster is reproducible for a given seed.

---

## Phase 4 — Planning UI (at-home tasks + expeditions + map) *(GDD §4, §7, §8, §15)*

**Goal:** Player can plan a week — assign at-home tasks, launch expeditions, and on Away weeks choose Pillage or Assault.

**Deliverables**
- [x] `Expedition` data class (`scripts/data/expedition.gd`) + `active_expedition` field on `MapTile`.
- [x] `WorldMapView` (`scripts/ui/world_map_view.gd`) — reusable GridContainer-based 15×15 map. Tile colour by terrain, Unknown tiles greyed, castles overlaid in red, town in gold, active expeditions show "Xw" remaining; emits `tile_clicked(x,y)`.
- [x] `scenes/screens/planning.tscn` — event banner, resources line, per-unit Defend/Train picker (with current value shown), checkbox to add a unit to the next expedition party, clickable map + selected-tile info, separate Explore/Gather launch buttons with full validation, active-expedition list.
- [x] Away-week sub-section (visible only when current event = Away Battle): Pillage / Assault Castle buttons, castle dropdown filtered to Explored-tile castles only.
- [x] `GameState.launch_expedition()` + `complete_expedition()` (Phase 5 uses the latter) + away-week pending fields (`pending_away_party`, `pending_away_mode`, `pending_assault_castle`).
- [x] Advance Time button — commits each at-home unit's task, walks the phase machine through Tick → Pre-Battle → Resolution (stubs for now), advances the week counter, rolls the next event, and re-renders. Phases 5–7 will replace the stubs.
- [x] Roster-view Continue button wired to Planning.

**Done when:** player can place all 4 units onto tasks/expeditions, validation prevents impossible plans (e.g. assigning a unit on expedition), and clicking Advance Time hands off to the Tick phase.

**Notes / known limitations**
- Mountain → Copper Ore via adjacent-tile gather (GDD §4 footnote) is **deferred**. Gather currently requires the *selected* tile to yield its own resource. MVP copper comes from castle assaults and pillage instead.
- The Advance Time button currently walks all four phases in one click because Phases 5–7 are not yet wired. Once Phase 5 lands, Tick will pause for the Pre-Battle Review screen instead of running through.

---

## Phase 5 — Tick & Pre-Battle Review *(GDD §5, §8, §12)*

**Goal:** Plan commit → world state actually changes → player gets a review window before the battle.

**Deliverables**
- [x] Training resolver — `+1` to target stat (capped 20, capped by remaining PA); small Det-rolled bonus +1 chance to another stat.
- [x] Expedition timer tick — decrement, on hit-zero deliver yield (`base_tile_yield × (1 + Σstrength/30)`), reveal Explored tile + castle if any, return units to home pool.
- [x] `scenes/screens/pre_battle_review.tscn` — post-Tick roster snapshot.
- [x] Formation editor — 4-0-0 slot picker (Blue / Green / Yellow / Red), per-slot OptionButton picker with slot-match `[match]` marker, live forecast.
- [x] "To Battle" button → hands off to Phase 6's resolver.

**Done when:** training and expedition returns mutate `GameState` correctly during Tick, the review screen shows the post-Tick state, and the formation editor records assignments. ✓

**Notes**
- Implemented as OptionButton-per-slot (with single-unit-per-slot enforcement on pick), not drag-and-drop. Same outcome with less UI plumbing.
- The Pre-Battle Review's setup pane is event-aware: formation editor for Away / Home / Bandit Ambush, champion + target-stat picker for Champion's Duel, up-to-4 participant checkboxes for Tournaments, a note for Bountiful Harvest / Merchant Caravan.

---

## Phase 6 — Combat resolution + events *(GDD §6, §13)*

**Goal:** Every event type can resolve end-to-end and produce a Weekly Summary.

**Deliverables**
- [x] Formation-battle math — `unit_power = 5 + Str + Bra + skill + slot_bonus + leadership_buff`, intimidation reduction of enemy total, Defend=full / other-home-tasks=×0.75, expedition units absent.
- [x] Pillage (`20 + week×3`), Home (`25 + week×4`), Assault (castle's fixed difficulty); won-castle removal from the world.
- [x] Battle Event templates: Bandit Ambush, Travelling Champion's Duel (single-unit `Str + Bra + Sword` vs `20 + week×2`, +1 to a chosen stat on win), Bountiful Harvest, Merchant Caravan.
- [x] `scenes/screens/battle_log.tscn` — per-unit contribution + final totals.
- [x] `scenes/screens/weekly_summary.tscn` — stat deltas, returned expeditions, rewards, "Next Week" button.
- [x] `scenes/screens/game_over.tscn` — triggered by Home Battle loss with cause + run stats.

**Done when:** any rolled event can play out from Planning through Weekly Summary, rewards land in `GameState.resources`, and a Home Battle loss ends the run. ✓

**Notes**
- Resolution is split: `scripts/systems/combat.gd` (pure formulas + enemy-power constants), `scripts/systems/battle_event.gd` (sub-type roll, harvest/caravan/duel), `scripts/systems/resolution.gd` (orchestrator + reward delivery). Combat doesn't touch GameState — it takes participants in, gives a breakdown out. Resolution is the only mutator.
- Slot-match is binary per GDD §12's "MVP simplification: no 1-slot-away rule"; rules live in `Combat.is_slot_match` and are tunable in one place.
- Merchant Caravan picker fires on the Weekly Summary (3 randomised bundles); Next Week is disabled until the player commits.

---

## Phase 7 — Tournaments & endgame *(GDD §6, §13, §16)*

**Goal:** The win condition exists and can be reached.

**Deliverables**
- [x] Tournament override at week 12N (already routed by Phase 2; finalise UI).
- [x] Tournament resolution — `unit_power = 10 + Str + Tec + max(Sword, Arch)`, enemy `60 + tournament_number × 25`, up to 4 participants, no formation editor.
- [x] Etiquette reward modifier — `reward × (1 + highest_Etiquette/40)` on Tournament wins.
- [x] `tournament_streak` increments on win / resets on loss.
- [x] Grand Tournament substitution after 2 consecutive wins (enemy `200 + year × 50`).
- [x] `scenes/screens/run_win.tscn` and final-summary version of game_over for the Grand Tournament loss case.

**Done when:** a full playable line from week 1 → Grand Tournament outcome is reachable in a single run. ✓

**Notes**
- Grand Tournament uses `Calendar.run_year(week)` (1-based years-since-start, not 1627+) — matches GDD §13's sanity check (`200 + year×50 = 250` at week 36).
- A lost Grand Tournament resets the streak and continues the run, per GDD §6 ("Lose: counter resets to 0; continue playing"). Only Home Battle loss ends the run.

---

## Phase 8 — Convergence, harness & tuning *(restructured 2026-06-10)*

**Goal:** One combat model where every stat and the formation matter, an automated way to measure balance, then the tuning pass — ending in a playable MVP that lasts a satisfying number of weeks without being trivially won or impossible.

Restructured per the 2026-06-10 project review (see CLAUDE.md "Current State & Development Plan"): the review found the formation layer is preview-only (real battles run `CombatSim`, which ignores slots/leadership/intimidation) and five stats have no combat effect — those must converge before numbers are worth tuning. Content additions are frozen until 8c produces measurements.

**8a — Stats & UI overhaul**
- [ ] Condensed FM-style at-a-glance stat grid per character (UnitCard / Knight Overview / chooser).
- [ ] Final stat list decided; every surviving stat has a mechanical job (Leadership / Intimidation / Loyalty / Etiquette / Horsemanship currently do nothing in the sim).
- [ ] Strategy layer (training targets, slot-match rules, tooltips) updated to match.

**8b — Sim alignment (one combat model)**
- [ ] Formation slots wired into `CombatUnit` / `CombatSim` (slot effects, Blue leadership aura, Intimidation vs morale pool).
- [x] `Combat.resolve_formation` retired — formation editor readout now derives per-unit battle ratings from `CombatUnit` and the Tactics tab forecasts via `CombatSim.analyze`, so preview and fight share math (2026-06-12).
- [ ] Tournament model decision: keep deterministic totals or fold into the sim.

**8c — Headless harness & autonomous loop**
- [x] Smoke runner: auto-play N seeded weeks headless, fail on script errors. (Pulled ahead of 8a/8b to de-risk them — shipped 2026-06-10 as `tools/smoke.sh` + `scripts/dev/smoke_runner.gd`, with a determinism-replay check and a cloud Godot bootstrap (`tools/get_godot.sh` + SessionStart hook).)
- [ ] Balance harness: scripted policies (train-weakest / always-defend / balanced), N seeded runs, metrics for week-reached, win rate, gold curve, injury frequency, `DEV_PACE` growth.
- [ ] Monte-Carlo `CombatSim` win-probability curves per week per enemy template.

**8d — Tuning pass (the original Phase 8)**
- [ ] Run full playthroughs (Knight pick → ending). Capture week reached + outcome.
- [ ] Adjust enemy power curves / castle difficulty per GDD §13's sanity check.
- [ ] Adjust gather-yield base values + Strength scaling if resource scarcity is wrong.
- [ ] UI polish — readable battle log, clearer event preview, consistent panel theming.
- [ ] Bug-fix pass on rough edges flagged during playtest.

**8e — Cleanup**
- [ ] Split `planning.gd` (~2,000 lines) into per-tab controllers.
- [ ] Regression-test the `STAT_CAP + 5` save clamp (body-type cap bumps surviving save/load).
- [ ] Unfreeze content additions.

**Done when:** a fresh tester can finish a run without confusion, outcomes feel earned rather than coin-flippy, and the harness can verify a balance change without a human playthrough.

---

## Progress Log

*Newest entry first. Add a dated line each session that ships code.*

- **2026-06-12 (continuation: cleanup, seed display, ATTENTION.md register)** — (1) Pre-battle `_battle_enemy_power` trimmed to the deterministic trio (tournament/Grand/duel) — the formation arms were unreachable after the scouting report; investigating the trim surfaced **B1, the biggest open design gap: castle assaults roll the same enemy party as a pillage** — `Castle.difficulty` (30–200) has scaled only loot since the CombatSim migration, making the hardest castle exactly as dangerous as a roadside camp. Documented, deliberately not patched (numbers belong to 8c). (2) F1 toolbar title row shows `seed · week` — the fact every bug report needs. (3) **`ATTENTION.md`** created at repo root: the triage register — short-form tables (A: Jack's decisions, B: design gaps, C: balance red flags, D: tech debt, E: tooling, F: watch list) + in-depth notes per item, cross-linked from CLAUDE.md's layout. Notable depth entries: B1 (assault difficulty), B4 (units born at PA ceiling — telemetry-confirmed), C3 (turn-cap measurement before tuning), D4 (orphaned power curves could become the assault party-budget function, solving B1).

- **2026-06-12 (autonomous polish run — nine tweaks, one pacing discovery)** — Free-rein improvement session, every item harness-validated. **(1) Pre-Battle scouting report**: formation battles no longer quote the legacy "Enemy strength: N" curves the sim never reads — scouts now name the actual preview party with relational strength words ("Bandit Leader (formidable), 2× Bandit (a fair match)"), rated against the household's average so the words stay meaningful across weeks; tournaments/duels keep their number (true there). **(2) Regional gather surfaced**: the map tile pane's Yield line now lists explored borders by terrain ("+ borders: Mountain ×2, Hills") with a count-only teaser for unscouted ones — the most strategic part of gather placement was invisible, and naming fogged terrain would have leaked. **(3) Standing orders, FM-style**: the weekly task reset to Defend made players re-pick training on all four units every week (staged dev needs ~4.5 weeks/point) — one forgotten click silently traded training for Defend; units mid-training now hold their order. **(4) Knight icon tooltips** end with "Fits: Yellow (Heavy Melee), …" from is_slot_match. **(5) Marshal's counsel on the chooser**: names the squire pool's two weakest axes so the knight pick is an informed complement, not mental stat-averaging. **(6) Knight Overview STAT_BLURBS** aligned with the unit-card honesty pass (found by a screen-layer audit that otherwise came back clean). **(7) Heal All Injuries** button on the F1 toolbar. **(8) README** rewritten from 7 stale lines to an honest feature list. **(9) Growth telemetry** in smoke summary lines (`growth +N`) — and its first battery exposed the pacing problem the stat proposal predicts: 20–37-week runs gain +0 to +13 visible points; seed 1628 played 33 weeks with **zero** growth, consistent with squire PA (60–140) overlapping the 12-stat starting sum (~84), meaning some units can never grow. Strong evidence for the proposal's PA-rescale item.

- **2026-06-12 (formation menu + research polish + the fight finally rendered)** — Three UI threads, no mechanics changed. **(1) Formation editor honest at last:** the "Projected combat power" readout (last caller of the dead strategy formula) is replaced by sim-derived per-unit battle ratings (`CombatUnit` → the same expected-damage × effective-HP score `CombatSim.analyze` uses, scaled for display), with unslotted-but-fighting units listed honestly; new `set_forecast_context(event_key)` adds a live win-chance line vs `EnemyDB.preview_party` — wired on the Tactics tab (Defense default forecasts vs this week's home raid, Attack vs a pillage camp). **`Combat.resolve_formation` deleted** along with its orphaned constants and `_slot_skill` (`is_slot_match` survives for ★ markers + oath fallback) — plan step 3's "one combat model" line is crossed off; slots feeding the sim remains with the stat overhaul. GDD §13 formula marked superseded-in-implementation with a changelog entry. **(2) Weekly Summary renders the actual fight:** formation battles used to show only "Totals — X vs Y" while the whole tactical story sat unused on `result["sim_result"]`. New `_render_sim_rows` table (combatant / dealt / taken / fate — "slain"/"felled" or hp remaining, crits and dodges noted) plus up to four chronicle-voiced highlight beats mined from the turn log (first blood, felling blows, the heaviest hit). The dead `_render_formation_rows` legacy table is gone. Totals line rephrased as "Strength remaining". **(3) Research tab:** gold-rimmed cells for projects affordable *this week*, costs in tooltips, a "Studied N of M · K within reach" strip, and disabled Research buttons now name their blocker (locked vs N gold short). Validated: full battery + UI probe (planning/pre-battle/weekly-summary instantiate the new code paths), boot clean.

- **2026-06-10 (smoke harness in the dev UI — SmokeEngine extraction + F1 toolbar section)** — The auto-player moved out of the CLI shell into `scripts/dev/smoke_engine.gd` (`SmokeEngine extends Node`, emits `progress` lines), shared by two front ends: `smoke_runner.gd` is now a thin arg-parsing shell over it (CLI behaviour unchanged — `tools/smoke.sh` output identical), and the **F1 Dev Toolbar gained a "Smoke Harness" section** — seeds/weeks spinners, a "Probe screens" toggle (probed screens flash over the game, deliberately), a Run button, and a streaming output panel that tints green/red on PASS/FAIL. In-game runs park the live campaign via new `SaveManager.snapshot_state()` / `restore_snapshot()` (in-memory wrappers over the existing serialise/restore — no disk), then reload the current scene so it re-reads the restored state; with no active run, leftovers are wiped so the title stays honest. Verified by a throwaway headless integration test (snapshot round-trip incl. roster identity; toolbar run + cleanup branch; deleted after passing). Also: `tools/get_godot.sh` now rebuilds the class cache when any script is newer than the cache file — a freshly added `class_name` (SmokeEngine, in this case) used to silently break headless runs until an editor pass.

- **2026-06-10 (UI probe — the harness now instantiates the real screens)** — The smoke runner bypassed the UI entirely, and the UI is the least-exercised code in the project (no human has played past week ~3). New `--ui` probe pass (on by default, `--no-ui` to skip): replays one run and instantiates the actual screen scenes at the moments a player would see them — knight chooser at run start; planning / roster / knight overview at week start; pre-battle after the tick; weekly summary after resolution; game-over on the real loss; run_win synthetically after game-over for crash coverage (the naive policy never wins the Grand). Probes weeks 1–3, every 5th, tournament weeks, and the final week. Screen `_ready` crashes surface as SCRIPT ERROR lines the `tools/smoke.sh` wrapper greps for — verified by planting a canary null-deref in roster_view (wrapper FAILed, runner alone wouldn't have) and restoring. Result: every screen instantiates cleanly across full runs on multiple seed ranges — the screens are healthier than expected. Probe runs separate from the determinism battery since screens legitimately consume RNG (forecasts, formation seeding).

- **2026-06-10 (bug & polish sweep — audited by harness + three parallel reviews)** — Full-project correctness pass while the stat redesign sits with Jack. Validation: 70-run smoke stress (40×100w + three 10×80w seed ranges), F6 dev scenes, headless boot — all green before and after. **Fixes:** (1) **Oath honour was mostly dead on the real battle path** — `result["per_unit"]` is deliberately empty on the CombatSim path, but six of twelve oath kinds read it: Swordsmanship/Archery/Leadership/Intimidation oaths could never fire, Bravery/Speed only via tournaments. The hidden +3 PA bonus and ⚜ notes silently never happened for most knights. OathLedger now reads the sim's `combatant_stats` (already on `result["sim_result"]`) for participation, and slot oaths fall back to `gs.formation` assignment + `Combat.is_slot_match`. (2) **Castles could generate on impassable Ocean tiles** — unreachable, unexplorable, unassaultable dead content the chronicle still counted; worldgen now skips impassable tiles (changes worldgen RNG consumption — pre-fix saves regenerate a shifted world, same accepted degradation as the pre-seed-fix saves). (3) **`iron_haul` away reward paid silently** — mutated inventory directly instead of `result["reward"]`, so it never showed on the Weekly Summary or history; routed through the reward dict. (4) **`world_dump.tscn` crashed since 2026-05-28** — still called the deleted `ResourceBundle.describe()` on `Castle.reward`; routed through `ResourceDB.describe`. (5) **Stat tooltips lied** — Speed/Technique/Bravery described the retired strategy formula, Loyalty claimed it "underpins Determination", Horsemanship promised unwired expedition bonuses, Leadership/Intimidation described preview-only math; combat lines now match `CombatUnit._derive`, parked stats get character descriptions only. **Audited clean:** save/load round-trip (all run-state fields, dev-state dicts, STAT_CAP+5 bumps survive; only stub `upgrade_costs` unpersisted), tick/economy/crafting (every resource id cross-referenced valid), reward tables, item drops, enemy drops, calendar math, injury healing, CombatSim (enemy ids offset +100, no tracking collisions).

- **2026-06-10 (smoke runner + cloud Godot bootstrap + determinism fix)** — Phase 8c's smoke runner shipped, pulled ahead of 8a/8b to act as the regression net under the stats/sim overhaul. **(1) Cloud Godot bootstrap.** `tools/get_godot.sh` downloads + caches the official 4.6.1 Linux binary (`~/.cache/godot`), rebuilds the class cache on fresh clones (`.godot/` is gitignored), and offers a `--check` boot check; a committed SessionStart hook (`.claude/settings.json`, with a `.gitignore` carve-out) runs it automatically so every cloud session can validate and play the game headless — the old "cloud sessions can't run Godot" limitation is gone. **(2) Smoke runner.** `scripts/dev/smoke_runner.gd` (run through `scenes/dev/smoke_run.tscn` — `--script` mode can't compile autoload-referencing project code) auto-plays N seeded runs × M weeks driving the same calls the screens make, with a naive policy: train-lowest-stat, Defend on home weeks, pillage with the full party (assault the weakest known castle every 8th week), best-duelist champion picks, all-in tournaments, formation slots filled in roster order, and an Explore/Gather expedition every few quiet weeks. Fails on engine error lines (via the `tools/smoke.sh` wrapper's grep), invariant breaches (week stuck, roster ≠ 4, negative gold, orphaned expedition task), and determinism mismatches (first seed replayed, traces compared). Never calls `SaveManager.save_game()`. Default battery 10 seeds × 60 weeks runs in ~1 s. **(3) First catch — a real determinism bug.** `RosterGenerator._taken_names` (static) was only cleared inside `roll_starting_squires()`, but both `main.gd` new-run paths roll knight candidates FIRST — so any run after the first in an app session rolled knights against the previous run's name list, consuming extra RNG draws and desyncing the whole seeded stream (and squires never avoided knight-candidate names). Fixed by `RosterGenerator.reset_names()` called from `GameState.start_run()`; the clear inside `roll_starting_squires` is gone, making the roll order irrelevant. New CLAUDE.md pitfall documents the class of bug. **Balance signal (not yet acted on):** under the naive policy all 10 seeds lose to a Home Battle around week 23–35 — the 8c balance harness will chart this curve once 8a/8b land.

- **2026-06-10 (project review + CLAUDE.md rewrite + Phase 8 restructure)** — Full architecture review session. **Findings:** (1) the formation layer is currently decorative — all real formation battles resolve through `CombatSim.run` on `CombatUnit`s, which derive from stats + weapon + armour only; slot bonuses, slot-match, the Blue leadership buff, and Intimidation reduction live solely in `Combat.resolve_formation`, whose only remaining caller is the formation editor's preview (called with `enemy_power=0`). (2) Five of twelve stats (Leadership, Intimidation, Loyalty, Etiquette, Horsemanship) have no effect in the sim — marked "reserved" in `combat_unit.gd`. (3) CLAUDE.md had gone stale — it still documented `ResourceBundle` (deleted 2026-05-28) as a live pitfall and omitted `CombatSim`/`CombatUnit`/`EnemyActor`/`RewardTableDB`/`Crafting`/`UserPrefs` entirely. **Shipped:** CLAUDE.md rewritten from scratch — accurate architecture summary ("How a Battle Actually Resolves"), new Known Issues section, updated repo layout + touch-point cheat sheet, the agreed five-step development plan, and a content freeze until the harness produces numbers. Phase 8 restructured into 8a–8e (stats & UI overhaul → sim alignment → headless harness → tuning → cleanup) per the plan agreed with Jack. No gameplay code changed this session.

- **2026-05-28 (resource system overhaul — unified Dictionary shape, RewardTableDB, enemy drops, regional gather)** — Big structural pass on the resource system per the 2026-05-28 design call. **`ResourceBundle` is gone.** Every reward roller (Combat, BattleEvent, StoryEventDB, Castle pre-roll, Resolution combat-event kinds, Crafting Caravan path) now returns a `Dictionary` keyed by ResourceDB ids — same shape as `GameState.inventory`. The dual-key namespace (`wood`/`fibres` vs `logs`/`plant_fibres`) is dissolved; `to_inventory_dict()` translation is gone with the class. New helpers on `ResourceDB` (`merge`, `scale`, `subtract_from`, `bundle_is_empty`, `describe`) do the four operations the old class wrapped, with `describe()` sorting tier-ascending for stable display. **New `scripts/data/reward_table_db.gd`** holds the data-driven loot pools — 13 tables shipped (wilderness_loot, mountain_loot, hill_loot, homestead_defence, bandit_pouch, tournament_prize, harvest, caravan_offer, plus 5 terrain-keyed gather tables). Each pool entry declares `amount: [w1_lo, w1_hi, w40_lo, w40_hi]` so progression scaling is per-entry interpolation; `roll(table_id, week, difficulty_mult)` and `roll_blended(weighted_tables, week, mult)` are the two public surfaces. Combat rollers become one-line wrappers — retuning is one number on one entry. **Two axes of scaling baked in**: week (auto-progression, internal to the table) and per-call difficulty multiplier (castles pass `difficulty / 100`, tournaments fold the Etiquette factor and tournament-number scalar, combat events pass a per-entry scalar). **Castle.reward is now a Dictionary** pre-rolled at world gen from a terrain-aware table (mountain-dominant → mountain_loot; hill-dominant → hill_loot; else → wilderness_loot) × `difficulty / 100`. World-dump determinism check updated to compare dicts. **Enemy-driven mob drops**: `EnemyDB.ENEMY_TYPES` gained a typed `drops` array per entry — every existing enemy now declares what dies-drops-what (goblins → goblin_hide + bone_shard, dire wolves → wolf_pelt, orcs → scrap_iron + iron_ore, spiders → spider_web, trolls → troll_bile + bone_shard, etc.). `EnemyDB.roll_drops_for(type_id)` rolls one enemy's drops; `Resolution._roll_spoils_from_enemies(enemy_cus, result)` aggregates spoils across every dead enemy on win, surfaces them as `result["spoils"]`. Folded into `_fill_from_sim` so every existing combat resolver (pillage, home, ambush, village_raid, tavern_riot, away custom modes, combat events) picks them up at the same point — no per-resolver wiring. **Regional gather** (Jack's call — adjacency special case scrapped): `Tick._complete_one` for GATHER now rolls the target tile's `gather_table_id()` at full weight + each Chebyshev-1 neighbour's table at `GATHER_NEIGHBOUR_WEIGHT = 0.3`, all blended into one Dictionary via `RewardTableDB.roll_blended`. Strength still scales the final dict. Mountain is now passable for gather (no more special adjacency rule). Tile placement now matters strategically — a forest tile next to two mountains brings logs *and* a little ore. **Seven new T1 raw materials** (goblin_hide, wolf_pelt, bone_shard, scrap_iron, feathers, troll_bile, strange_relic) anchor the enemy drops; **three new T2 recipes** (quilted_padding, feather_arrow, reforged_blade) consume them so combat loot feeds back into the crafting loop. **UI updates**: Weekly Summary renders Reward + Spoils on separate lines (encounter loot vs kill loot, distinct prose), Planning castle labels route through `ResourceDB.describe`, Caravan picker reads dicts directly, game_state history captures spoils on the chronicle line. **Files changed**: ~16 across data, autoload, systems, screens, dev; `scripts/data/resource_bundle.gd` deleted (+ .uid). **Next up**: validation, then content sweep (more drops on new enemies, more T2 recipes consuming the raws).

- **2026-05-27 (bug-fix + QoL batch — formation seed, debt sticky flag, sort determinism, planning state survival)** — Polish pass focused on real bugs found in a static audit. (1) **Formation defaults seeded for every formation-using sub-type**, not just `bandit_ambush`. `GameState.current_event_uses_formation()` now returns true for `village_raid`, `tavern_riot`, and every `CombatEventDB.has_mode()` entry — so the Tactics-tab default Attack / Defense formation actually auto-seeds on those weeks instead of leaving the Pre-Battle editor empty. (2) **Forecast now mirrors Resolution for non-Defend penalty.** `pre_battle_review._is_home_battle()` only honoured the 0.75× modifier on non-Defend units for `bandit_ambush`, but `Resolution._resolve_village_raid` / `_resolve_tavern_riot` / `_resolve_combat_event` all use `_player_cus_home` — so those weeks were giving optimistically-high forecasts that didn't match the actual sim. Function extended to cover the same set as fix (1). (3) **`maintenance_debt` was a sticky flag.** `Tick._apply_gold_maintenance` set `gs.maintenance_debt = true` on a short week but never cleared it. `Chronicle._event_line` reads it every week and emits *"The ledger fell short this week. The household will feel it."* — so once the player went into debt once, that line appeared every chronicle entry for the rest of the run, even on weeks where wages were paid in full. Now reset to `false` whenever funds cover upkeep, so the flag is per-week truth. (4) **`CombatSim` sort comparator non-determinism.** The initiative-jitter lambda was calling `RNG.randi_range(0, 2)` *inside* the sort comparator — meaning the same pair `(a, b)` could compare both ways across consecutive calls, violating sort invariants. The jitter is now rolled once per combatant before the sort and stored on the new `CombatUnit._init_jitter` field; the comparator is a pure stable key compare. Same gameplay feel, deterministic ordering. (5) **Planning state survives scene changes.** `_pending_tasks` was a local Dict on `planning.gd` — clicking a knight's name to open Knight Overview reloaded Planning on return, wiping the dict and defaulting everyone back to Defend. Moved to `GameState.pending_tasks` (cleared by `_clear_week_buffers()`), so per-unit assignments persist for the whole planning week regardless of how the player navigates around. **Validation:** headless run before fixes printed `[KM27] Title ready.` clean; the user should re-run to confirm a clean state post-fixes. **Next up:** Phase 8 balance pass — first playthroughs to ground-truth enemy curves and gather yields.

- **2026-05-27 (staged stat development + FM-style dev arrows)** — Stats no longer jump N→N+1 the instant a training week succeeds. `scripts/data/stats.gd` gains a hidden accumulator: `add_progress(stat, points, PA, extra_cap)` feeds *fractional* development, scaled by `DEV_PACE` (~4.5 weeks/point at full rate) and a headroom factor (`_headroom_factor`) that shrinks as the unit's total nears its Potential Ability — so growth tapers near potential and the arrow simply fades, never exposing PA (GDD §10). A whole accumulated point ticks the integer and carries the remainder. **Every in-run gain now routes through it** (per Jack's "stage everything"): weekly training + the §7 training-bonus roll (`Tick._apply_training`), the every-4-weeks Determination roll (`Determination.roll_for_units`), the Travelling Champion's Duel + refugee/petition/tavern nudges (`Resolution`, 4 sites), and story-event `random_unit_stat` / `all_units_stat` grants (`StoryEventDB._apply_stat_delta`). Start-of-run roster generation, house leans, body bumps, and dev-toolbar edits still set integers directly — those are the baseline, not development. **Show-not-tell arrows**: two hidden recency dicts — `momentum` (set to `MOMENTUM_WEEKS=3` on a level-up → bright ▲ "recently improved") and `developing` (set to `DEV_ACTIVE_WEEKS=2` on any progress feed → small ▲ "developing", driven by *recency of input* not residual carry so it's honest) — both aged once per week by `Stats.decay_development()` at the top of `Tick.apply`. Injured stats show ▼. `Stats.development_state(stat, PA, injured)` returns the state; `development_glyph/color/tooltip` render it. Arrows surface on every `UnitCard` row and on `knight_overview` (no numbers, no bars — the arrow is the whole message). `SaveManager` persists `progress` / `momentum` / `developing`; old saves load empty (no arrows until they develop). All downstream readers updated for the staged semantics: `weekly_summary`, `pre_battle_review`, the `WeekProcessor` beats, and `oath_ledger._trained_successfully` (the oath now honours *productive* training — gained-or-developing — rather than the rare integer tick). Pace + arrow constants are deliberately grouped as Phase-8 tuning knobs. **Next up:** Phase 8 balance — ground-truth `DEV_PACE` against real playthroughs.

- **2026-05-27 (FM-style week processor + tab routing fix)** — Advancing the week is no longer an instant cut to Pre-Battle. New `scripts/ui/week_processor.gd` (`WeekProcessor`, a `CanvasLayer` overlay) plays an FM "processing" sweep after `Tick.apply()`: a procedurally-drawn spinner (asset-free, `_draw` arc — same ethos as `BannerIcon`) turns while the week's beats reveal one at a time on a ~0.55s cadence — **Upkeep & Coffers**, **Training Yard**, **Stirring of Resolve** (Determination weeks), **Expeditions Return**, **Infirmary**, and a closing **Week Ahead**. Notable beats pause and wait for a click / Enter, mirroring how FM stops the schedule for news: unpaid wages, a discovered castle, and the battle ahead all halt the sweep. Clicking during the auto-reveal skips to the next beat. `planning.gd::_do_advance` now builds the beat list from the tick-result Dictionary (`_build_week_steps`), disables its own hotkeys while the overlay owns input, and hands the overlay an `on_finished` callback that does the scene change to Pre-Battle. **Bug fix in the same pass**: the Research (and any) tab could feel "locked in" — the Calendar lives on `content` index 5 but isn't an entry in the external `TabBar`, so opening it left the TabBar highlighting the prior tab; re-clicking that already-highlighted tab didn't fire `tab_changed`, trapping you on Calendar with a desynced highlight. Tab selection now routes through `tab_clicked` (fires even on re-click) into a unified `_select_main_tab(idx)` that always clears Calendar state and re-syncs the highlight; keyboard 1–5 call the same helper. **Next up:** staged-integer stat progression with FM-style development arrows (show-not-tell), per Jack's follow-up.

- **2026-05-20 (per-run house lean randomisation)** — Each run rolls a fresh stat slant for each of the four houses. The houses keep their **identities** (Brann always reads warrior; Aldermere always reads scholar) but the specific +1/-1 stats now come from archetype-themed pools rather than a fixed list. **Pools per archetype**: warrior plus from {strength, swordsmanship, bravery, intimidation, determination} / minus from {etiquette, technique, leadership, archery}; scholar plus from {etiquette, leadership, loyalty, technique, determination} / minus from {strength, intimidation, swordsmanship, speed}; scout plus from {speed, archery, technique, horsemanship, intimidation} / minus from {bravery, leadership, strength, etiquette}; cavalier plus from {horsemanship, bravery, leadership, swordsmanship, etiquette} / minus from {archery, determination, technique, intimidation}. `HousePool.roll_per_run_leans()` picks 3 plus + 2 minus per house at `GameState.start_run` time, through the seeded RNG (same seed → same slants). New `GameState.house_leans: Dictionary` carries the picks; `HousePool.apply_lean(stats, house_id, stat_max, per_run_leans)` reads from it (falls back to the static `HOUSES.lean_plus / lean_minus` when the dict is empty — preserves old-save behaviour). `RosterGenerator._roll_knight/_roll_squire` thread `GameState.house_leans` through. `SaveManager` persists `house_leans`; old saves without the field keep the fresh start_run roll. **Net effect**: your Brann knights might feel intimidation-heavy this run, then bravery-leaning the next; your Aldermere might emphasise loyalty over leadership one run, etiquette over technique another. House lean stays implicit (no chip, no tooltip — per the GDD §9 design rule), but each campaign's roster has a slightly different shape. CLAUDE.md cheat sheet gains a row for the new pool location.

- **2026-05-20 (enriched run-end screens — chronicler's closing)** — Both `game_over.gd` and `run_win.gd` were bare lists of facts; they now read like the chronicler's closing pages. New `Chronicle.generate_run_epitaph(gs, outcome)` composes a three-line reflection — tone-set (3 win openers, 3 loss openers), household summary (Knight's name + an oath-flavoured short clause like "the keeper of faith" / "the unbroken-backed" / "the riser-from-floors" + their final reputation band), and a closing verdict (castles taken + tournament streak for loss, or the realm-won line for win). Both screens add: heralded `❦ The Record` and `❦ The Household` section blocks, fleuron dividers between sections (matching the chronicle aesthetic on the chooser / overview cards), reputation surfaced as `12 (Respected)`, season label baked into the week-reached line, and per-unit chronicle entries showing name + epithet + trait + oath stat-key + stat total. Pure additive — no save-format change, no GameState change; just turns the moment-of-conclusion from a stat-readout into a chronicled ending.

- **2026-05-20 (four house-themed events + event-level house gate)** — Houses (Brann / Aldermere / Daven / Faldur) had stat leans and crests but no events keyed to them. New event-level gate `requires_house_in_roster: <house_id>` enters the `StoryEventDB` schema; an event with this key only enters the roll pool when some unit in the roster bears that house. Four events ship with it: `brann_old_rival` (a Brann knight calls out an old grudge for a single sparring bout), `aldermere_letter` (court news + small inheritance + a polite favour that costs more than it offers), `daven_scout_report` (a Daven scout brings hard-won intelligence + a cache or an injury), `faldur_horse_sale` (a Faldur breeder arrives with a string of horses; your Faldur member negotiates as one of the breed). Catalog goes 88 → 92. Houses now feel like genuine lineages with chronicle weight, not just stat-shift labels. The gate complements the existing outcome-level `requires_trait_in_roster`.

- **2026-05-20 (seasons surfaced — six-season chip on every main header)** — `Calendar.season_for(week)` carves the 48-week year into six labels (Early Spring → Late Spring → Summer → Harvest → Autumn → Winter) at the same boundaries `Chronicle._season_clause` was already using for prose. `Calendar.season_chip(week)` formats it with a small glyph (❁ / ❀ / ☀ / 🌾 / 🍂 / ❄). Planning's `ContextLabel`, Pre-Battle Review's header, and Weekly Summary's header now all carry the chip so the player feels the year pass even outside the Calendar tab. Prose was already season-flavoured; this gives the eye a one-glance anchor.

- **2026-05-20 (chronicler asides — atmospheric one-liners between events)** — Each week's `Chronicle.generate_week_entry` now has a 22% chance to append a short parenthetical "aside" — a one-line atmospheric beat that has nothing to do with the week's main events. Pool of 20 entries: the kitchen acquires a cat, the marshal receives a letter he will not discuss, the chaplain loses an argument about ale rationing, the stable lad begins writing poems (reviews are mixed), the chronicler's quill splits mid-entry on Thursday, a comet that turns out to be a lantern, etc. Pure flavour, no mechanical effect — builds the sense of a household with a life beyond the player's direct attention. Pure additive in `Chronicle.gd`; no schema change.

- **2026-05-20 (bard's ballad — dynamic per-knight chronicle prose)** — New `Chronicle.generate_household_ballad(gs) -> String` composes a personalised 3-line ballad from the Knight's chronicle data: oath_kind picks the opener (12 templates — "Hear of {n}, who kept faith while the gate yet held —" / "Hear of {n}, who rode the lists for cause and result —" / etc.), earned epithet OR personal trait picks the middle (epithets get a 3-template pool, traits get one of 14 trait-specific lines), and the household's current reputation band picks the close (7 verdicts, from "and his name is sung at the small fires of every village in the realm." down to "and the bard, on second thought, accepts only a smaller fee for the song."). New `StoryEventDB` effect kind `bard_ballad` calls the generator and surfaces the ballad as a single multi-line note on the Weekly Summary. New `bard_ballad` event entry (min_week 20, costs 4–8 gold for the bard's fee, grants +3–6 reputation + +4–9 PA + the dynamic ballad note). Catalog goes 87 → 88. Pure additive; works with any combination of trait / epithet / reputation.

- **2026-05-20 (festival week + trait-gated outcomes + 5 whimsical events)** — A new optional outcome filter `requires_trait_in_roster: <trait_id>` enters the `StoryEventDB` schema. Outcomes carrying this key only enter the weighted pool when **some** unit in the roster has that trait — the chronicler only writes the brawl line when there's actually a Hot-Headed knight to throw the punch. Resolver gains a small `_filter_outcomes(outcomes, gs)` + `_roster_has_trait(gs, trait_id)` helper; outcomes without the key resolve unchanged. **Five new events** lean into the new mechanic for character-keyed payoff: `festival_week` (the flagship — five base outcomes + five trait-gated vignettes: Hot-Headed starts a brawl, Lucky wins a coin-toss spree, Silver-Tongue ends as unofficial herald of two villages, Tournament-Brat sweeps the impromptu lists, Pious reads at the bonfire), `the_stable_cat` (the household's old cat yowls at the back gate; resolves as instinct vindicated, nothing of note, or a small funeral under the rowan), `the_practical_joker` (boiled eggs in the marshal's boots; one branch is gated to Hot-Headed escalation that ends well), `sleepy_squire` (re-assigned to dawn watch; a Lucky-gated branch where coincidence is a kind of grace), `midsummer_long_evening` (a household-binding scene around a midsummer fire). Catalog goes 82 → 87. Pure additive — events without the new key work exactly as before; the gating mechanism is invisible to old events.

- **2026-05-20 (polish — oath notes always surface on the Weekly Summary)** — Small visibility fix. The Weekly Summary's chronicle / battle-breakdown section previously hid itself when there was nothing combat-y to show; that meant oath-honour `⚜` notes (and any other `result["notes"]` chronicle beats) were silently swallowed on non-combat weeks. `_render_battle_breakdown` now also considers `has_notes` when deciding whether to render the section, and headers as `❦ Chronicle` when the only payload is prose. No new logic — just routes the existing notes channel to the screen on every week that has anything to say. Players see their oath honours on Bountiful Harvest, Merchant Caravan, and other non-combat weeks instead of only when combat happened to fire alongside.

- **2026-05-20 (content sweep — 2 combat + 2 away + 4 story + 2 traits)** — Pure data additions across the four catalogs. **Combat events** (`CombatEventDB.EVENTS`) grow 3 → 5: `midnight_skirmish` (twenty riders, bandit_ambush template, +2/-1 rep, Uncommon item drop, min_week 10) and `ogre_in_the_hills` (single hard target, home_battle template, +5/-3 rep, 30% Rare drop, min_week 20). **Away modes** (`AwayModeDB.MODES`) grow 8 → 10: `hunt_rogue_mage` (single dangerous target, +4/-2 rep, min_week 14) and `escort_diplomat` (decent purse + standing, +3/-2 rep, min_week 8). **Story events** (`StoryEventDB.EVENTS`) grow 78 → 82: `sworn_sister_arrives` (old friend's quiet attention; pure non-stat-check), `quiet_anniversary` (private reflection, big PA + loyalty / lonely fallout), `borrowed_book_returns` (technique + PA), `long_winter_warning` (prep for autumn — buy grain, dig wells, brace). **Traits** (`TraitPool.TRAITS`) grow 12 → 14: `silver_tongue` (+2 Etq, +1 Lea, −1 Swd) and `haunted` (+1 Spd/Det/Loy, −1 Bra, +10 PA — high-PA trait at low weight). All pure data, all new entries land through existing dispatchers.

- **2026-05-20 (CombatEventDB framework + 3 new combat sub-types)** — Battle-event combat sub-types are now data-driven. New `scripts/data/combat_event_db.gd` ships with three entries: `harpy_raid` (home_battle template, +4/−2 rep, home_defence item drop, min_week 14), `goblin_warband` (pillage template, bandit bundle, ambush item drop, +2/−1 rep, min_week 8), `cultist_incursion` (pillage template, 8–18 gold + bundle, 35% Rare item drop chance, +3/−2 rep, min_week 18). Each entry declares `combat_template`, `reward_kind` (bandit_bundle / home_bundle / gold_and_bundle / gold_only), `item_drop_fn` (ambush / home_defence / rare_biased / none), `epithet_tag`, `rep_on_win/loss/no_defender`, intro / no-defender / flavour / tactics-advice / stakes strings, and `min_week`. **Strictly additive**: `Resolution._resolve_battle_event` checks `CombatEventDB.has_mode(sub_type)` and routes to a new `_resolve_combat_event` that mirrors the legacy bandit_ambush / village_raid / tavern_riot path (no-defender → CombatSim → injuries → win-branch with reward + survival epithets + WON loop + item drop + rep). The three legacy hard-coded combat resolvers are deliberately NOT migrated — they keep bit-for-bit behaviour. Migration is pure data work, deferred. `BattleEvent.ROLL_POOL` grows: 2 `combat_event` gateway entries added (≈15% of battle-event weeks), eligible ids gated by `min_week`. Pre-Battle Review threads intro + enemy power + forecast template + flavour text + stakes text; Planning Tactics tab gains advice line. `BattleEvent.label()` + `is_combat()` extended. **Adding a 4th combat sub-type is now one dict entry** — no Resolution code needed. CLAUDE.md cheat sheet gains a row for the new touch-point. Save format unchanged.

- **2026-05-20 (oath honour mechanic — v1, honour-only)** — Each unit's sworn oath sentence now has mechanical weight. New `Unit.oath_kind` field captures the stat key the oath was drawn from at creation (via `Chronicle.derive_oath_kind`) so honour checks have a stable reference even if the unit's highest stat shifts mid-campaign. New `scripts/systems/oath_ledger.gd` runs at the end of `Resolution.run` (when not game-over): per-roster check, +3 hidden PA + one `⚜ X honoured his oath this week.` chronicle line per honoured unit. **All 12 oath stat axes covered** with cheap per-stat signals: loyalty → defended at home; bravery → won combat without injury; determination → trained this week; swordsmanship → slot-matched in Yellow/Red on a win; archery → Green slot win; horsemanship → on expedition or in away party; leadership → Blue slot in a win; etiquette → tournament participant in a win; strength → on a Gather expedition; speed → in any combat un-injured; technique → training successfully landed (cap + PA didn't block); intimidation → in any formation combat. **Honour-only for v1** — no break penalties; missing the trigger costs nothing. SaveManager persists `oath_kind` with back-derivation fallback for older saves. CLAUDE.md cheat sheet gains two new touch-point rows (body type bumps + oath ledger). Pure additive — no UI changes; the only player-facing signal is the chronicle note on the Weekly Summary.

- **2026-05-20 (body type implicit stat-cap bumps)** — The four body silhouettes (Lean / Burly / Tall / Wiry) were pure visuals; they now carry **implicit +1 stat-cap bumps** on two stats apiece, same design philosophy as house leans (no tooltip, no chip, no visible number — the player learns through play). `BodyType` gains a `CAP_BUMPS` dict and two helpers: `cap_bump_for(body_type, stat)` and `cap_bumps(body_type)`. `Stats.try_increment` and `Stats.try_increment_random_excluding` gain a new `extra_cap` / `extra_caps` optional param (default 0 / {} preserves existing behaviour). All eight `try_increment` call sites — Tick training + det bonus, Determination weekly roll, Resolution's champion duel reward + caravan witness + petition host + tavern riot loyalty tick, StoryEventDB stat delta — now thread the body's bump through. `Stats.set_value` clamp loosened from `STAT_CAP` to `STAT_CAP + 5` so a Burly knight whose Strength reached 21 in-play survives a save/reload round-trip. Body cap bumps: Lean (+Speed, +Technique), Burly (+Strength, +Intimidation), Tall (+Archery, +Horsemanship), Wiry (+Speed, +Swordsmanship). **Pure additive** — old behaviour intact for any path that doesn't pass a body type.

- **2026-05-20 (epithet trigger coverage — TAG_HOME_BATTLE_SURVIVED wired)** — The previously-dead survival epithet pool ("the Bulwark", "the Wall", "the Unfleeing", "the Last-Standing") is now granted on close-margin home-side wins. Trigger: `OutcomeBracket.Bracket.ORANGE` on a winning sim — the player's HP fell below 50% of starting but still beat the enemy. New helper `Resolution._maybe_grant_survival_epithets(party, bracket, injuries, result)` runs **before** the existing `TAG_HOME_BATTLE_WON` loop in `_resolve_home`, `_resolve_bandit_ambush`, `_resolve_village_raid`, `_resolve_tavern_riot` — so survival epithets (the more dramatic story) take precedence over the generic win tag (`Chronicle.grant_epithet` respects "one epithet per unit", so the WON loop fills any units the survival check skipped). Survival epithets only go to units who weren't injured this fight — "the Wall" doesn't read right on someone who took a hoof to the ribs. Pure additive change; survival tag pool was already defined.

- **2026-05-20 (4 more stat_check events across less-used stat axes)** — `calm_spooked_horse` (Horsemanship best 9 — pure data add for an axis nothing else exercises), `lead_the_charge` (Bravery best 12 — high stakes, rep ±4–7, injury on failure), `outguess_card_player` (Knight's Technique 10 — gold gain + PA boost vs steady gold drain), `hold_line_at_drill` (Loyalty all_avg 8 — household-wide test that rewards or drains discipline across the roster). Catalog goes 74 → 78. All on the existing stat_check primitive; pure data adds.

- **2026-05-20 (stat_check primitive + 6 decision events)** — Story events can now declare an outcome that branches on a stat threshold. The schema is opt-in via a new `stat_check` key inside an outcome; outcomes without it resolve exactly as before (the 68 existing events are unchanged). When present the resolver picks the at-home roster's `best`, `knight`, or `all_avg` value of the named stat, compares against the threshold, and substitutes the on_pass / on_fail branch's `note` + `effects` for the outcome's own. A small `(Best Leadership carried the test — 14 vs 12.)` chronicle line is appended so the player feels the check land instead of guessing why the outcome happened. Six new events use it: `mutiny_brewing` (Leadership 12), `negotiate_dispute` (Etiquette 12), `stare_down_rival` (Intimidation 11), `endure_long_march` (Determination avg 9), `read_the_forged_letter` (Technique 12), `read_the_room` (Knight's Etiquette 11). Catalog goes 68 → 74. Each branch carries flavourful prose + effects sized to the stake — failing the Leadership check on Mutiny costs −2 Loyalty on a random unit and a gold drain; passing it gives +1 Loyalty across the roster. **Next up:** more stat_check events, or one of the other small mechanics (oath honour/break, body type hooks, epithet trigger coverage).

- **2026-05-20 (framework lifts — Palette expansion + UiStyle builders)** — Centralised UI styling pass. (1) **Palette expanded** with castle card bg/border (idle vs target), difficulty band colours (light/medium/heavy/formidable), stat descriptor band tints (wretched→outstanding), reward + outcome label tints (gold purse / won / lost / neutral), shadow panel + deep shadow, chronicle prose colours (origin / oath / banner / trait), and a `DEBT` / `DISABLED_TEXT` pair. ~35 named constants total. (2) **New `scripts/ui/ui_style.gd` builder** with six factory functions for the most-repeated `StyleBoxFlat` patterns: `chip()` (rounded 6 px panel with 2-px border), `card()` (chip + 12/8 content margins), `slot()` (chip with default 2 px, used by SlotDropZone), `knight_tile()` (KnightIcon background with hard ink border), `swatch()` (small 4 px corner icon background), `progress_fill()` + `progress_bg()` (Pre-Battle Review win-prob gauge). (3) **Sweep across the high-traffic widgets**: `SlotDropZone` (3 states → 3 lines of UiStyle.slot), `KnightIcon` (12-line block → 1 line UiStyle.knight_tile), `PoolDropZone` (12-line block → 1 line UiStyle.chip), `planning.gd` tournament chip (28 lines → 11 lines), castle card (18 lines → 7 lines), recipe swatch (12 lines → 3 lines), research icon + empty cell (12-line + 12-line blocks → 4 + 3 lines), and Pre-Battle Review's win-prob gauge (16 lines → 2 lines). `_castle_difficulty_color()` now reads Palette constants. Total ~60 lines of stylebox boilerplate removed; **adding a new chip / card / progress bar is now a one-liner** that's automatically consistent with the rest of the medieval dress. Future tinting passes will touch one file. CLAUDE.md cheat sheet updated with both touch points. **Next up:** another framework lift (CombatEventDB) or one of the small mechanics (stat_check, oath honour/break, body type hooks).

- **2026-05-20 (home missions — first combat sub-types)** — Two new battle-event combat sub-types added to `BattleEvent.SUB_TYPES`: `village_raid` (A Village Under Attack — uses `home_battle` enemy template, gold + bundle + Rare item drop chance + +4 rep on win, −2 rep on loss; rides out with all at-home units) and `tavern_riot` (A Tavern Riot — uses `bandit_ambush` template, small purse + +1 Loyalty roster-wide + +1 rep on win, −1 rep on loss). `BattleEvent.is_combat()` extended to include both, so the existing combat path UI fires. `ROLL_POOL` grows 9 → 11 entries (3 story + 8 hard-coded) to keep story-event share roughly a quarter of battle event weeks. New resolvers in `Resolution`: `_resolve_village_raid` and `_resolve_tavern_riot`, both mirroring `_resolve_bandit_ambush`'s structure (party check → CombatSim → injuries → rewards + epithet + item drop + rep adjustment) with mode-specific rewards and prose. Pre-Battle Review threads setup pane + stakes text + enemy power + forecast template + enemy flavour text for each. Planning Tactics tab gains advice lines for each. **Adding a 3rd combat sub-type** is the same shape — add to SUB_TYPES/ROLL_POOL, label, is_combat, write a _resolve_X mirroring the existing pattern, and wire pre-battle + planning. A data-driven `CombatEventDB` would reduce that to a single data entry, similar to `AwayModeDB`; deferred until a third sub-type lands. **Next up:** more combat sub-types, more traits, polish, or stopping for sanity-check.

- **2026-05-20 (catalog expansion — 4 weapons + 3 armours mid-tier)** — The mid-tier drop pool was thinning visible repeats with the wave of new Uncommon/Rare-biased away modes. Four new weapons: `dueling_dagger` (Uncommon, +5 hit, 10% crit — duelist's special), `horseman_axe` (Uncommon, balanced cavalry axe), `bastard_sword` (Rare, hand-and-a-half, dmg 6–12 +2 hit), `arbalest` (Rare, steel-armed crossbow, dmg 10–16). Three new armours: `gambeson` (Uncommon Light, layered linen), `mail_hauberk` (Uncommon Medium, knee-length rings), `field_plate` (Rare Heavy, dmg-absorb 13). Weapons go 17 → 21 entries; armours 12 → 15. New distribution: weapons 8 Common / 5 Uncommon / 6 Rare / 2 Heirloom; armours 5 / 5 / 3 / 2. No code changes — all entries fit existing `Weapon.CATALOGUE` / `Armour.CATALOGUE` schemas and inherit power_rating + rarity + flavour fields. **Next up:** stopping for sanity-check unless asked to keep going.

- **2026-05-20 (away mission variants — 5 more modes + wrapping UI)** — Pure data additions to `AwayModeDB.MODES`: `capture_scout_captain` (light combat, decent purse + maps, min_week 10), `explore_catacombs` (hard combat, best-odds Rare item drop, min_week 18), `raid_cultist_site` (hard combat, big purse, rep ±5 on win, ±2 on loss, min_week 18), `investigate_meteor` (light combat, 4–7 iron haul, +3 rep, min_week 22). Catalog grows 4 → 9 away modes total. Planning Map tab's variants row swapped to `HFlowContainer` so 8+ unlocked buttons wrap cleanly at 1280-wide instead of overflowing. **Next up:** more away modes, weapons / armours, or moving to home-mission combat sub-types when the user is back to sanity-check the heavier paths.

- **2026-05-20 (away mission variants framework)** — Pillage and Assault are now joined by data-driven away mission variants. New `scripts/data/away_mode_db.gd` ships with four entries: `rescue_merchant` (gold + bundle, +3 rep, Uncommon item bias), `hunt_beast` (iron + fibres + small gold, +2 rep, lighter bandit-ambush combat template), `destroy_nest` (3–6 iron + +4 rep + Rare item bias on pillage-strength combat), `intercept_convoy` (24–44 gold + bundle, +2 rep, neighbour stakes). Each entry declares its own `combat_template` (selects which `EnemyDB` party rolls), `reward_kind` (gold_and_bundle / iron_haul / rare_loot), `rep_on_win` / `rep_on_loss`, `min_week`, `epithet_tag`, glyph, label, intro, and Planning Map tooltip. **Critically additive**: `Resolution._resolve_away` early-returns into a new `_resolve_away_custom` only when `AwayModeDB.has_mode(pending_away_mode)`; the existing pillage/assault path is bit-for-bit untouched so castle capture, item drops, epithets, and rep adjustments for those two modes are unchanged. New helper `_resolve_away_custom` mirrors the pillage path's structure (combat sim → injuries → reward + epithet + item drop + rep) but reads every parameter from the mode entry. Item drops on custom modes route through a new public `ItemDrops.drop_at_rarity()` helper biased to the mode's `item_drop_rarity` (caps at Rare; Heirlooms stay Grand-Tournament-only). Planning Map tab grows a second row "Other targets in the marches:" with one button per mode unlocked at the current week. Pre-Battle Review threads the mode's intro into the setup pane, picks the correct enemy-power and forecast template, and prefixes the header with the mode label. Weekly Summary header also shows the mode label. **Adding a 5th away variant is now pure data** — drop a key into `MODES`, no resolution code touched. **Next up:** more away modes / home mission sub-types, or expand item / squire systems further.

- **2026-05-20 (home-mission events + reputation band crossings)** — Six new "home mission flavour" events resolved as story events (no new combat path): `hunt_forest_beast`, `missing_child_in_woods`, `repair_village_defences`, `investigate_haunted_mill`, `clear_infested_mine`, `deliver_urgent_medicine`. All carry reputation deltas + situational stat/inventory effects. Catalog 62 → 68 entries. New `GameState.adjust_reputation(delta) -> String` helper returns the new band label when a delta crosses a band boundary (Outcast / Disreputable / Suspect / Known / Respected / Renowned / Legendary). `StoryEventDB._apply_reputation_delta` and every direct `gs.reputation` mutation in `Resolution` (six call sites — assault +4, pillage +1, away loss −1, home defence +3, tournament +2, grand tournament +8) now route through it. When a band is crossed the result notes get a one-liner: `→ The realm now calls you Respected.` from story events or `→ Standing crossed: now Respected.` from combat resolution. Players see their rep arc become discrete narrative beats instead of a number ticking on a chip. **Next up:** pause point unless more low-risk work is wanted — the away mission variants framework is the next big lift.

- **2026-05-20 (reputation mechanical hook + 14 more events)** — Reputation now has bite, not just narrative weight. Combat resolution (`scripts/systems/resolution.gd`) bumps the chip on every natural beat: +2 on a regular tournament win, +8 on Grand Tournament, +4 on castle assault, +3 on home defence, +1 on pillage, −1 on an away loss. Tournament purses also scale: `rep_bonus = clampi(reputation / 4, 0, 25)` is added to the prize, with the bonus called out in the result note so the player sees where the extra gold came from. Cap of +25 keeps legendary status from breaking economy. Then a fourth event batch — 14 more Tier-A entries covering the bulk of the user's remaining wishlist: `ambushed_on_the_road`, `equipment_rusts_in_rain`, `missing_scout`, `corrupted_well`, `cave_in_blocks_route`, `strange_whispers_in_camp`, `enemy_scouts_observed`, `ancient_curse_unearthed`, `ancient_shrine`, `time_moves_strangely`, `merchant_cursed_relics`, `dragon_overhead`, `eclipse_darkens_the_region`, `strange_obelisk`. Catalog goes 48 → 62 entries. All on existing primitives — most of the rare/atmospheric beats now have homes. **Next up:** Away Mission Variants framework (data-driven away modes) or a sweep of equipment-flavour items if more low-risk work is wanted.

- **2026-05-20 (reputation system + 8 reputation events)** — New `GameState.reputation: int` (default 0, cleared on `start_run`, persisted by `SaveManager` with backwards-compatible default-0 load). Two new effect primitives in `StoryEventDB`: `reputation` (flat delta) and `reputation_range` (rolled delta). `ResourceDB.resource_hud_bbcode()` gains an optional `reputation: int = 0` third arg that prepends a `❦ Rep: N (Band)` chip; bands `Outcast / Disreputable / Suspect / Known / Respected / Renowned / Legendary` map onto roughly −30/−10/0/10/20/40 boundaries with darker → warmer → bright-gold tinting. All four HUD callers (Planning, Pre-Battle Review, Roster View, Weekly Summary — including its multi-spot refreshes) now thread `GameState.reputation`. Eight new events drive the new primitive: `local_festival`, `travelling_bard`, `rival_rumours`, `village_attacked`, `train_local_militia`, `protect_harvest_festival`, `defend_trade_caravan`, `rescue_lost_pilgrim`. Catalog goes 40 → 48 entries. Reputation has narrative weight only at this stage — future passes can tie it to tournament reward scaling, mercenary recruit odds, and event gating. **Next up:** more events (intel / world-flag systems), or the Away Mission Variants framework.

- **2026-05-20 (story events — batch 2)** — Thirteen more Tier-A data adds drawn from the user's positive/negative/rare lists, all using existing effect primitives: `wandering_swordsman`, `retired_veteran`, `forgotten_battlefield`, `captured_beast_escapes`, `lost_caravan_cache`, `travelling_merchant`, `desertion_in_the_night`, `plague_rats`, `drunken_brawl`, `fire_in_the_supply_wagon`, `bandits_steal_livestock`, `reality_tear`, `dice_dealer`. Catalog goes 27 → 40 entries. Mix of positive (5), negative (5), rare (3). No new code paths — all events lean on `gold` / `gold_range` / `random_unit_stat` / `all_units_stat` / `random_unit_injury` / `reward_resources` / `inventory_add` / `inventory_remove` / `pa_delta`. **Next up:** reputation system + reputation-touching events, or a third event batch if quicker.

- **2026-05-20 (story events — primitive expansion + first event batch)** — Three new effect primitives in `scripts/data/story_event_db.gd`: `clear_injury` (heals the longest-running injury on a random injured at-home unit), `inventory_remove` (negative `inventory_add`, clamps at zero so a flavour event can't push the household into resource debt), `expedition_delay` (adds 1–2 weeks to a random active expedition's `weeks_remaining`). All three are pure additions to `_apply_effect` and unblock a wide cluster of events the user listed. Nine new event entries shipped with them — `hidden_springs`, `wandering_alchemist`, `food_stores_spoil`, `goblin_sabotage`, `sudden_storm`, `tax_collector`, `talking_crow`, `mimic_treasure_cart`, `ghost_army`. The catalog goes 18 → 27 entries. The gateway weight (story_event 3× in `BattleEvent.ROLL_POOL`) is unchanged; the larger pool just means each story id surfaces less often per run, more variety across a campaign. **Next up:** another wave of Tier-A data events from the user's lists, then the reputation system.

- **2026-05-20 (story event system — data-driven chronicle moments)** — New `scripts/data/story_event_db.gd` ships **18 story events** (plague_year, traveling_minstrel, dispute_in_the_yard, wandering_pilgrim, midnight_messenger, drinking_contest, broken_blade, lord_demands_tax, abandoned_armoury, old_rival, omen_of_battle, runaway_squire, the_chronicler_speaks, frost_in_spring, night_alarms, forge_master_visits, memorial_for_the_fallen, bardic_contest). Each is a pure data entry: label, intro prose, weight, gates (`min_week` / `max_week` / `min_gold` / `min_roster_at_home`), and 2–3 weighted outcomes. Each outcome carries a chronicle paragraph + a list of **effect primitives** the resolver applies: `gold`, `gold_range`, `random_unit_stat`, `all_units_stat`, `random_unit_injury`, `reward_resources`, `inventory_add`, `pa_delta`. **Adding a new event is pure data** — drop a key into `EVENTS`, the resolver and UI handle the rest. **Gateway integration**: `BattleEvent.SUB_TYPES` keeps its 6 hard-coded entries; a new `ROLL_POOL` weights `"story_event"` 3× so stories take roughly a third of battle event weeks (≈11% of all weeks). When the gateway lands, `StoryEventDB.roll_event_id(gs)` picks an eligible story given current week/gold/at-home roster; if no story passes the gates, the roller falls back to a hard-coded sub-type. The chosen story's id rides on `GameState.current_battle_event` as `"story:<id>"`; Resolution detects the prefix and dispatches to `StoryEventDB.resolve()`. **UI surfacing**: Pre-Battle Review shows the full intro prose under a wrapped simple-note (autowrap turned on for all simple notes so future long blurbs don't overflow). Planning's Tactics tab shows a "chronicle moment — no setup required" advice line. Weekly Summary detects the story prefix and renders the breakdown header as `❦ Chronicle` (instead of `Battle Breakdown`), shows the story label on the outcome row in warm gold, and lets the existing notes loop render the prose + effect lines. **Save-safe**: stories live entirely in `current_battle_event` as a string — no schema change. **Next up:** Phase 8 balance pass; per-event flavour variants if any single beat starts feeling repetitive across multi-run playthroughs.

- **2026-05-20 (knight backgrounds + event variety)** — Second of two playability batches. Each knight is now a more distinct character at roll time, and the battle-event rota grew. (1) **Traits.** New `scripts/data/trait_pool.gd` with 12 weighted trait entries (Veteran, Hot-Headed, Pious, Tournament Brat, Scholar Knight, Horse-Born, Marked, Lucky, Sworn Defender, Reluctant, Poacher, Stoic). Each carries `stat_mods` + `pa_mod` + a one-paragraph blurb. `Unit.trait_id` field added. `RosterGenerator._roll_knight/_roll_squire` rolls a trait after the house lean and applies it (clamped to STAT_CAP for knights, SQUIRE_STAT_MAX+2 for squires so a trait can't push squires too far out of their roll band). `SaveManager` persists `trait_id` with validity guard so a renamed trait in a newer build doesn't leave a broken descriptor on a loaded knight. (2) **UI surfacing.** UnitCard shows a "❖ Trait Name" chip below the house line with the blurb in the tooltip. Knight Overview's chronicle card shows the full name + paragraph in the heraldic header block. (3) **Origin pool doubling.** Chronicle's knight origins grow 9 → 18, squire origins grow 8 → 16, so the chooser sees fresh prose more often across runs. Repeated playthroughs no longer exhaust the pool by run 2. (4) **Epithet pool expansion.** 19 → 32 epithets across the 7 tags — every win tag now has at least 4 options, the dramatic ones (tournament, grand) have 5+. The dead `home_battle_survived` tag is now stocked (4 options) and ready to wire when a partial-loss home defence path lands. (5) **Two new battle event sub-types.** `refugee_caravan` (Refugees at the Gate — weighted three-way outcome: sheltered costs gold + may earn a witness +1 Loyalty; passing leaves a small cloth/timber gift; turned away is flavour only) and `noble_petition` (A Noble's Petition — 55% an envoy's purse + a host's +1 Etiquette, 45% a courtesy visit with no effect). Both are pure non-combat events that resolve automatically with chronicle notes; Pre-Battle Review carries setup notes + stakes text; Planning Tactics tab gains advice lines. BattleEvent.SUB_TYPES grows 4 → 6, so a battle-event week now picks among six possibilities (≈17% each instead of 25%). **Next up:** Phase 8 balance pass, plus the open future-direction items (Tier-2 enemy activation, oath honour/break, house-keyed chronicle prose).

- **2026-05-20 (item system — visibility + loot pipeline + strategy-layer effect)** — Weapons and armours moved from invisible data into a real progression system. (1) **Catalog expansion.** `Weapon.gd` grows from 9 → 17 entries (added arming_sword, dueling_sword, greatsword, war_pick, halberd, warbow + 2 Heirlooms: ancestral_blade, master_warbow). `Armour.gd` grows from 6 → 12 entries (added studded_leather, scale_mail, brigandine, plated_mail + 2 Heirlooms: ornate_breastplate, warden_harness). Each entry now carries `power_rating` (0–4, integer, the strategy-layer kit bonus), `rarity` (Common / Uncommon / Rare / Heirloom), and `flavour` (short prose blurb). New helpers: `Weapon.describe()` + `Armour.describe()` give one-line stat readouts ("Longsword — dmg 5–10, +1 hit, +3% crit, +2 power"); `ids_of_rarity()` returns id arrays for loot pools. (2) **Strategy-layer effect.** `Combat.resolve_formation()` adds `weapon_power + armour_power` to each unit's raw before slot/leadership multipliers — formation battles now reward kit alongside stats. `Combat.tournament_unit_power()` adds the same, gated by `_is_tournament_legal()` which forbids war_pick and crossbow (historical lists rules; players can still field them in normal weeks). (3) **Loot pipeline.** New `ItemDrops` (`scripts/systems/item_drops.gd`) rolls drops on each win path: pillage 18% (Common-heavy), assault 45% (Uncommon-heavy with Rare ceiling), home defence 12%, ambush 20%, tournament 30% (Uncommon-or-Rare), Grand Tournament guaranteed Heirloom. Drops add to `GameState.item_stockpile: Array[{slot, id}]` — the household armoury, persisted by `SaveManager`. (4) **UI surfacing.** Every `UnitCard` now shows a compact "⚔ Weapon · 🛡 Armour" line, each side rarity-tinted; full tooltip carries the stat lines. Knight Overview gains a new "Equipment" block under Current Status — current weapon + armour with describe() lines, flavour quotes, and a Change… button per slot that opens a popup of stockpile alternatives. Equipping swaps: old item returns to stockpile, nothing is lost. Weekly Summary's rewards list now renders the dropped item with rarity tint ("⚔ Ancestral Blade (Heirloom) — added to the armoury"). Tournament breakdown rows show "(+N kit)" when equipment contributes. (5) **Save compat.** Backwards-compatible — saves without `item_stockpile` load cleanly with an empty armoury. **Next up:** knight backgrounds + traits + new event sub-types (batch 2).

- **2026-05-20 (QoL + visual polish batch)** — Fifteen items across visual polish, QoL clarity, and input/accessibility, shipped as one batch. **Visual / animation polish:** (1) `ScreenFade.fade_in()` helper used by every major screen (`planning`, `pre_battle_review`, `knight_overview`, `knight_chooser`, `roster_view`, `game_over`, `run_win`) — 0.22 s wash on entry so scene changes never snap. Weekly Summary keeps its own per-section stagger animation untouched. (2) **UnitCard hover lift** — `mouse_entered` brightens the card via a pure-`modulate` Tween (no layout shift), `mouse_exited` snaps it back; prior tween killed on every toggle so fast in/out never sticks. (3) **Win-probability gauge bar** on Pre-Battle Review — horizontal `ProgressBar` styled with `OutcomeBracket.color_for()`, value tweens to the new percentage in 0.18 s so each slot tweak registers. Existing "Forecast: X% chance" label stays underneath. (4) **Projected formation power readout** on the formation editor — calls the same `Combat.resolve_formation()` the real battle uses with `enemy_power=0`, surfacing per-slot total + grand total + slots-filled chip. Updates live as units are dropped, dragged out, or right-click reassigned. (5) **Slot-match preview during drag** — `SlotDropZone.set_preview_match()` paints a gold glow + warm bed (distinct from the post-drop green-match style) on slots whose bonus axis matches the dragged knight. Wired via `NOTIFICATION_DRAG_BEGIN/END` on the editor, which reads `Viewport.gui_get_drag_data()` and ticks each zone. **QoL clarity:** (6) **Persistent tournament countdown chip** in the Planning top bar — always visible across all five tabs, colour-rampls from muted parchment ("Tournament in 6 weeks") through warm amber (≤2 weeks) to bright gold ("★ GRAND TOURNAMENT — THIS WEEK"). (7) **Crafting recipe rework** — recipes that are unlocked but unaffordable now appear with a disabled Craft button and a tooltip listing exactly how many of each material is short ("Need: Plant Fibres ×3 more"); name hover + inputs hover both surface the recipe summary so the player no longer has to click through to the info popup for every recipe. Short ingredients render red in the inputs line. (8) **Disabled Advance Time tooltip** + **Save Now button** in the settings popup with active-run gating. (9) **Quit / Return-to-Menu confirm guards** wrapping the destructive buttons via `ConfirmDialogUtil.ask()`; suppress-this-run still honoured. **Audio + input:** (10) `MasterAudio` autoload — three buses (Master/Music/SFX) added at runtime if not already present, exposed as 0–1 linear sliders in the settings popup, conversion to dB handled internally. A 60 ms procedural click SFX is synthesised on first use (44.1 kHz mono PCM, exponential decay over 880 + 1760 Hz blend) and pooled across four `AudioStreamPlayer`s so overlapping clicks never truncate. Played on save confirmation, confirm-dialog buttons, and the SFX slider's `drag_ended`. (11) `Palette` autoload — centralised dictionary of palette constants (gold ramp, parchment, semantic colours, slot-zone tints, tournament-chip ramp) so future re-tinting touches one file. Initial sweep converted `KnightIcon`, `SlotDropZone`, and the tournament chip to use it; wider sweep across screens deferred to a focused pass. (12) **Right-click → "Assign to slot…" popup** on `KnightIcon` inside a formation editor — alternative to drag-drop with a per-slot star marking slot-match. Includes "Return to pool" entry when the unit is already slotted. (13) **Keyboard shortcuts** — `Esc` closes the settings popup / info overlay / intro splash and returns from Knight Overview; `Enter` confirms ConfirmDialog and triggers the primary action on Planning + Pre-Battle + Weekly Summary; `1`–`5` switch Planning main tabs; `C` toggles Calendar; the confirm-dialog focus lands on the Confirm button so a player can blast through routine confirms. Spacebar / Enter on the Weekly Summary's first press skips the staggered fade and reveals every section instantly. (14) **Load-save confirmation** — `SaveManager.peek_save()` returns metadata (year, week, gold, streak) without restoring; the Title screen "Continue" button now opens a confirm dialog showing those numbers before clobbering the active run. `ConfirmDialogUtil.ask()` gained a `force` parameter so title-screen flows can prompt even when no active run is in memory. (15) Audio bus layout + the new autoloads (`Palette`, `MasterAudio`) registered in `project.godot`. **Next up:** Phase 8 balance pass — first playthroughs to ground-truth enemy curves and gather yields, plus extending the Palette sweep across the remaining ~12 screen scripts when a quiet session opens up.

- **2026-05-19 (research tree + map dropdown cull)** — Two threads. (1) **Research tree fleshed out.** Five suggested projects added — Forester's Guild (T2 forestry), Falconry (T2 husbandry), Cartography (T2 lore), Apothecary's Cabinet (T3 husbandry, requires falconry), Tannery (T3 cultivation, requires cotton + forester) — bringing the total to 8 across five categories: cultivation, forestry, metallurgy, husbandry, lore. Their `unlocks` reference future content not yet wired to `ResourceDB.RESOURCES`; the detail panel falls back to a prettified title-case rendering of the snake_case ID so they read cleanly until real recipes land. Categories live in two new constants (`RESEARCH_CATEGORIES`, `RESEARCH_CATEGORY_LABELS`) so the renderer never hardcodes names. **Layout suggestion implemented:** swimlane tree. The grid now reads as five horizontal category lanes × N tier columns (currently T2 + T3). Each cell is either a research icon (108×108 button, category-coloured) or a faint dotted empty cell so the structure of the tree reads at a glance. Tier headers sit above the grid; the category name labels the left edge of each lane. Right-hand detail panel stays. (2) **Map dropdown cull.** Two dropdowns gone. The per-unit task `OptionButton` (13 items × up to 4 units) is replaced with an **assignment chip** Button per row — its label shows the current task at a glance (`⛨  Defend the homestead`, `✦  Training Strength  (now 14)`, etc.); click opens a `PopupMenu` with Defend + the 12 stat-train rows. The away-battle `CheckBox` is replaced with the same toggle-button styling as the expedition-party row (`＋  Add to Away Battle` / `⚔  Riding to the Away Battle`). The castle `OptionButton` for assault-targeting is replaced with a stack of **castle cards** — each is a clickable Button styled as a card showing `🏰  (x,y)`, a difficulty band ("Lightly held" / "Garrisoned" / "Well-defended" / "A formidable seat") in a colour ramp, the reward bundle, and a `✓ Target` mark on the currently-selected one. **Next up:** Phase 8 balance pass.

- **2026-05-19 (visual polish batch)** — Five focused visual wins. (1) **Resource HUD strip.** Used to read "Gold: 100  — Fabric —  — Timber —  — Metal —" — the empty slots looked broken. Each slot now leads with the tier glyph (`◆` fabric / `▲` timber / `■` metal) in a muted lowest-tier colour when empty, so the bar reads as a deliberate inventory readout even at week 1. Gold gets its own `✦` mark. Held resources show in their actual tier colour with the same glyph prefix. (2) **Status glyphs on unit cards.** The "Status: idle · at home" / "Status: train:strength · at home" line was reading the raw task string verbatim — fine for debug, bad for play. New `_status_line_for(unit)` returns one of: `✚ Recovering`, `⚑ Abroad — expedition #N`, `✦ Training Strength`, `⛨ Defending the homestead`, `○ Idle — at home`. Glyph reads at a glance; the prose behind it carries the specifics. (3) **Calendar tab visual hierarchy.** History was a single text line per week. Now each row is a five-column layout: heraldic bullet (`❦`), year/week chip, event label, colour-coded outcome (green win, red loss, parchment for noncombat), and a dim score/reward tail. Faint amber rules separate weeks so the eye can land on one at a time. (4) **Heraldic fleuron prefix on section headers.** Roster, Situation Report, Assignments, Active Expeditions, Upcoming, History, Stats, Current Status, Recent Activity, Battle Breakdown, Rewards, Stat Changes, Expedition Returns, Raw Materials, the per-type recipe headers (Fabric / Timber / Metal), and the research tier rows all now carry `❦` in warm-gold (0.92, 0.78, 0.42). One voice across static tscn-defined and runtime-built headers via the shared `_styled_section_header()` helper. (5) **Fleuron chronicle dividers.** The plain `HSeparator` between stats and chronicle on chooser cards (and between sections on the Knight Overview chronicle card) is now a centred `❦` glyph flanked by two faint rules — reads like a manuscript section break instead of a generic line. **Next up:** Phase 8 balance pass.

- **2026-05-19 (self-directed polish batch)** — Free-rein polish session: six wins + a cleanup. (1) **Heraldry rule-of-tincture.** `Chronicle.generate_banner` used to emit "Lion Rampant Gules on gules" — metal-on-metal / colour-on-colour, instantly readable as wrong to anyone who's ever looked at a coat of arms. Each charge in the dictionary now carries a `metal_tincture` and `colour_tincture` template; the field's tincture decides which the charge uses, so the alternation is always correct. (2) **Battle Log absorbed into Weekly Summary.** Item 6 of the previous batch was only half-shipped (conditional skip when per-unit was empty); now `pre_battle_review._on_confirm` always routes to Weekly Summary, and the per-unit / tournament / duel breakdown + combat notes (including epithet grants — these were silently dropping in the conditional version) appear as a new animated section inside the existing reveal. Deleted `scenes/screens/battle_log.tscn` and `scripts/screens/battle_log.gd` — neither was reachable anymore. (3) **Crafting strict filter.** Recipes now only appear when both `is_craftable` (research-unlocked) AND `can_afford` (ingredients in hand) — the Research tab is where the player sees the road ahead, the Crafting tab is purely "what can I press the button on right now." Type headers only render if at least one recipe under them passes both gates. Empty-state copy reads "The workshop stands idle…" instead of "No recipes available." (4) **Map visual polish.** Fogged tiles now layer a faint diagonal hatch under the brighter `?` so they read as "the mist before the mist breaks" rather than just a slightly lighter tint. The expedition pennant got a backing disc and a proper circular countdown badge so it reads against any terrain colour at a glance. (5) **Medieval flavour strings.** Replaced bare "—" placeholders on the Weekly Summary (rewards, expedition returns), the Planning expedition list ("The grounds are quiet…"), and the Calendar tab ("The chronicle is unwritten…"). (6) **Disabled-action tooltips.** Launch Explore / Launch Gather buttons now carry a tooltip that explains exactly why they're disabled — pick a unit, click a tile, target's out of range, terrain yields nothing, etc. No more dead buttons that don't say why. **Next up:** Phase 8 balance pass.

- **2026-05-19 (six-item polish batch)** — Numbered feedback from a playtest. (1) **Fog-of-war.** Added a derived `is_fogged_in(world, x, y)` helper on `MapTile` — true when an UNKNOWN tile borders an EXPLORED one. No new persisted state, no save migration. `TileIcon` gained a `FOGGED` kind that renders a brighter, larger `?` over a slightly tinted background; `WorldMapView._make_tile_button` picks `FOGGED` vs `UNKNOWN` per tile. Planning's Explore action now gates on fogged status — scouts can only step into the next ring, so the map develops outward instead of letting you teleport to the far edge week one. Selection text reads "Fogged — within scouting reach" vs "Unknown — too distant to scout yet". (2) **Expedition party UI** — the bare `CheckBox` "Add to next expedition party" was the eyesore Jack flagged. Replaced with a full-width toggle Button: subdued "＋ Send on Expedition" off, gold "✓ In Expedition Party" on. Style updated in place via `_style_party_btn`. (3) **Crafting tab** — recipes that aren't `is_craftable` (research-locked) are now hidden entirely; the menu only shows what the player can actually craft right now (with the Craft button greying when ingredients are short). Each visible recipe gets a small tier-coloured swatch with a type glyph (`◆` fabric / `▲` timber / `■` metal) — the placeholder "icon" Jack asked for. (4) **Research tab — icon tree.** Rewrote the flat text list as a two-column body: tier-rowed icon grid on the left (each project is a 96×96 toggle button, tier-coloured by category, with a glyph indicating done / locked / available), detail panel on the right (description, prerequisites, unlock list, Research button) that fills in when an icon is selected. Added `category` / `tier` / `prerequisites` fields to `RESEARCH_PROJECTS`; `blast_furnace` now requires `alloy_research`. (5) **Event chip.** Year/week line in the planning header is no longer mashed together with the event — pure date now. The event moved to a parchment chip in the Overview tab header's top-right ("Upcoming · Home Battle"), so the player can always see what next-week's Advance Time will trigger. (6) **Intro splash made modal.** The "Your Journey Begins…" panel used to sit inline in the planning VBox, letting the cluttered Overview tab show through underneath. Moved it to be a root-level full-rect overlay (z_index 90) with a 92%-opaque backdrop + centred card, so hitting Begin is a clean reveal. (7) **Battle Log skip.** When `last_battle_result` has no per-unit data, no tournament participants, and isn't a duel, pre_battle_review now routes straight to Weekly Summary — the Battle Log was just showing "No participants — automatic forfeit" in those cases, which is duplicated by the Weekly Summary's outcome line. **Next up:** Phase 8 balance pass.

- **2026-05-19 (dead-code trim)** — Survey + delete of confirmed dead code. (1) `GameState.advance_to_next_week()` was a Phase-4-era stub that just delegated to `wrap_week()` and was kept "so older call sites keep compiling"; a grep shows zero callers anywhere in the project, so it goes. (2) Four "legacy helpers" in `EnemyDB` — `roll_enemy_group`, `group_power`, `roll_t1_group`, `describe_group` — comment claimed they were "kept for expedition flavor and old display code" but the only references are the declarations themselves. Drops ~40 lines from `enemy_db.gd`. The `ResourceBundle` ↔ `ResourceDB` dual-system is **deliberately left in place** — 9 files use the legacy triple and it's a documented active migration (see CLAUDE.md), worth a dedicated session rather than a quick trim. Headless validation still clean. **Next up:** Phase 8 balance pass, or the ResourceBundle migration when it gets prioritised.

- **2026-05-19 (chooser HBox overflow)** — Follow-up to the previous fix. Putting the "Take into service" button in the card's header row gave each knight card a ~270px wider min-width (the button's text "Take Tomas Whitehall into service" became part of the header's natural width). With three knight cards laid out horizontally, the row's combined min-width pushed past the 1280-wide viewport and the chooser content overflowed both edges — visible in a fresh playtest as the left and right cards being cropped while only the centre card was readable. Two changes: (1) `UnitCard.build` now renders the chooser button on its **own row** right under the header (still always visible, no scroll needed), with `size_flags_horizontal = EXPAND_FILL` so it spans the card; the button's natural width no longer drives the card width. (2) Tightened the stats grid — `name_lbl.custom_minimum_size.x` 110 → 96 (still fits "Determination" / "Swordsmanship"), grid `h_separation` 20 → 12, row `separation` 6 → 4 — cutting roughly 50px off each card's stats-grid min-width so three cards comfortably fit at 1280. **Next up:** Phase 8 balance pass.

- **2026-05-19 (overflow fixes after chronicle)** — Three follow-up fixes after the chronicle/heraldry additions made the unit cards tall enough to overflow several screens. (1) **Knight Chooser**: the "Take into service" button used to live at the bottom of the card under the chronicle paragraph and oath — past the visible viewport at 1080p, so the player couldn't get past the chooser. Moved it into the header row (`UnitCard.build`) next to the name, where it's always visible whether or not the chronicle scrolls. Squires and Planning-tab cards don't pass `on_choose`, so no button renders there. (2) **Planning Overview tab**: `RosterCards` was a bare `VBoxContainer` — the four 2×2-laid-out chronicled cards now overflow the tab without a scroll. Wrapped in `OverviewScroll` (`ScrollContainer`, horizontal_scroll_mode=DISABLED) and re-pointed the `@onready` in planning.gd. (3) **Knight Overview**: the 3-column `Columns` HBox had no scroll, so the heroic chronicle card on the left, the 4 stat-group panels in the middle, and the recent-activity column on the right could push content below the viewport. Wrapped Columns in `ColumnsScroll` and re-pointed every `@onready` in knight_overview.gd. **Next up:** Phase 8 balance pass or compacting card content further (heraldry text emits colour-on-colour combos).

- **2026-05-19 (bug fix + chooser polish)** — Three fixes pulled from a Knight-Chooser screenshot audit. (1) **Save/load bug** — `SaveManager._world_seed()` was returning `0` unconditionally, so every load regenerated the world with a *different* random seed; the saved `explored_tiles` and `castles_remaining` then got patched onto mismatched terrain (a Hills tile you'd gathered iron from could come back as Ocean). Now reads `GameState.world.seed_value` (already stamped by `WorldGenerator.generate`). `_restore_state` now uses `data.has("world_seed") and != 0` to detect a real seed instead of the `seed_val == 0 → randi()` fallback, so legitimate seed-0 saves wouldn't be quietly re-randomised either. (2) **Duplicate-surname collisions** in the starting roster — the audited screenshot showed two squires named *Carrowmoor* in different houses, which reads as a glitch. New `NamePool.random_name_avoiding(used_names)` rolls until both first name and surname are unique versus a passed-in list, with a 12-attempt fallback to the unconstrained roll. `RosterGenerator` holds a static `_taken_names` array cleared in `roll_starting_squires()` (always called before `roll_knight_candidates()` on the chooser), so all 6 cards on the chooser are guaranteed unique. (3) **Chooser scroll** — wrapped the SquiresRow/KnightsLabel/Cards block in a `ScrollContainer` so dense Knight cards (origin paragraph + oath) can't run off the bottom at 1080p. **Next up:** Phase 8 balance pass or the heraldry text generator emitting metal-on-metal / colour-on-colour combos (cosmetic).

- **2026-05-19 (combat infrastructure)** — **Tactical layer wired into resolution.** `EnemyActor` — duck-typed enemy unit satisfying CombatUnit's interface (id, unit_name, stats, weapon_id, armour_id). `EnemyDB` rewritten: all 9 enemy types now carry full 12-stat ranges + default_weapon_id/armour_id; `roll_combat_party(event_key, week)` builds real CombatUnit parties (uses RNG); `preview_party(event_key, week)` builds midpoint-average parties with no RNG (safe for UI). `CombatUnit` made duck-typed (untyped `unit` field), `power_mult` parameter added for home-battle 0.75× rule. `CombatSim` updated: random target selection (not lowest-HP), `turns_taken` removed from result, `analyze()` static added for fast RNG-free UI forecasts. `Resolution._resolve_away/home/_resolve_bandit_ambush` now run `CombatSim.run()` as the authoritative source of `won`; `sim_result` added to battle result dict; injury bracket derived from HP margins. `pre_battle_review._update_forecast()` replaced with `CombatSim.analyze()` against `EnemyDB.preview_party()`. **Next up:** migrate Battle Log to display sim turn_log; wire tournaments through CombatSim; expose equipment on Planning/UnitCard.

- **2026-05-19** — **Tactical combat simulation foundation.** Four new files. `Weapon` — static catalogue of 9 weapon entries (unarmed, shortsword, longsword, axe, spear, dagger, shortbow, longbow, crossbow) with damage range, hit_bonus, crit_bonus, primary_skill, and range_type. `Armour` — static catalogue of 6 armour tiers (unarmoured through full_plate) with base_rating, dodge_penalty, block_chance. `CombatUnit` — bridge class that takes a Unit + weapon/armour IDs and derives all combat-layer stats from strategy stats: max_hp (Str×3+Bra×2), initiative (Spd×2+Tec+skill), hit_chance (Tec+skill+weapon.hit_bonus), dodge_chance (Spd+Swd−armour.dodge_penalty), block_chance (armour.block_chance+Swd), crit_chance (Tec+weapon.crit_bonus), damage range (weapon_base+Str scaling), armour_value (armour.base_rating+Str/6), morale_pool (Bra×3+Det×2). Every previously-dead strategy stat now has a combat role; reserved notes added for Leadership, Intimidation, Loyalty, Etiquette, Horsemanship. `CombatSim` — turn-based engine: initiative-ordered action sequence, focus-fire targeting (lowest HP), hit→dodge→block→damage resolution with armour reduction, turn-limit fallback winner by remaining HP. Returns `{winner, turns_taken, attacker/defender_hp_remaining, combatant_stats, turn_log, notes}` for future Battle Log display. `Unit` gains `weapon_id` and `armour_id` string fields. `RosterGenerator` assigns starting kit (Knight: longsword+leather, Squire: shortsword+unarmoured). `SaveManager` serializes/deserializes both fields. **Next up:** wire CombatSim into Resolution as a parallel resolution path, or surface equipment on UnitCard/Planning.

- **2026-05-18 (mechanics harmony)** — **16-step mechanics cohesion audit, all steps shipped.** P1 (critical bugs): (1) `Determination.CHANCE_PER_POINT = 0.5` constant extracted — `tick.gd` now references it instead of duplicating the literal. (2) `resource_bundle.gd` — warning comment block added documenting the `wood/fibres` ↔ `logs/plant_fibres` key asymmetry and the `to_inventory_dict()` contract. (3) `resolution.gd` — game-over guard added before `_apply_reward()`; was applying rewards to ended runs. (4) `chronicle.gd` — seven `TAG_*` string constants added for all epithet grant sites. (5) `resolution.gd` — all bare tag-string literals replaced with `Chronicle.TAG_*` constants. P2 (formula extraction): (6) `combat.gd` — `static func tournament_unit_power(unit) -> int` named helper extracted (10+Str+Tec+max(Sword,Arch)); `resolve_tournament()` now calls it. (7) `pre_battle_review.gd` — participant power display uses `Combat.tournament_unit_power(u)`. (8) `expedition.gd` — `static func estimate_yield(party_strength) -> int` extracted from the inline `GATHER_BASE_YIELD × (1 + strength/30)` formula. (9) `tick.gd` — gather yield calls `Expedition.estimate_yield()`. (10) `planning.gd` — expedition forecast calls `Expedition.estimate_yield()`. P3 (crafting system): (11) New `scripts/systems/crafting.gd` — `Crafting.craft(gs, id)` and `Crafting.accept_caravan_offer(gs, idx)` centralise all inventory-mutation for crafting and caravan rewards. (12) `planning.gd._do_craft()` delegates to `Crafting.craft()`. (13) `game_state.gd.purchase_research()` helper added; `planning.gd._on_research()` uses it. (14) `weekly_summary.gd._on_caravan_pick()` delegates to `Crafting.accept_caravan_offer()` — caravan reward delivery now routes through the same layer as all other rewards. P4 (design-gap docs): (15) `enemy_db.gd TIER2_TYPES` — comment noting Phase 8 week-threshold activation plan. (16) `enemy_db.gd loot_tags` — comment noting reserved-for-future-loot-system status. **Next up:** Phase 8 balance pass.

- **2026-05-18** — **Polish & systems flesh-out pass.** Six files, 211 lines net. (1) `resolution.gd` — `Chronicle.grant_epithet()` was defined but never called anywhere; now fires at all win conditions: away-pillage (`pillage_win`), away-assault (`assault_win`), home-battle (`home_battle_won`), bandit-ambush (`home_battle_won`), champion-duel (`duel_win`), tournament (`tournament_win`), grand-tournament (`grand_tournament_win`). Newly earned epithets append to `result["notes"]` so they surface in the Battle Log. (2) `resource_db.gd` — added `RESEARCH_PROJECTS` const with three entries (`cotton_cultivation` / `alloy_research` / `blast_furnace`), each carrying name, description, gold cost, and a list of recipe IDs they unlock; keys match the existing `"research"` gate strings in `RESOURCES`. (3) `planning.gd` — replaced the static Research-tab stub with a live `_refresh_research_tab()` that renders each project with a gold-cost button, description, and "Unlocks:" list; purchasing deducts gold, appends to `GameState.researched`, and refreshes both Research and Crafting tabs. (4) `pre_battle_review.gd` — added `_enemy_flavor_text()` that draws on EnemyDB naming bands (week ≤8: goblins/bandits, ≤20: goblin-warriors/orcs, 20+: orc-veterans) and renders one contextual flavor line below the enemy-strength number. (5) `combat.gd` — replaced flat home-win `(2,2,1)` and bandit-ambush `(1,1,1)` bundles with week-scaled rolling formulas matching the pillage pattern. (6) `map_tile.gd` — Hills terrain now returns `"iron_ore"` from `gather_resource()` (previously returned nothing); `resource_db.gd` `iron_ore.map_source` corrected to `"hills"`. **Next up:** Phase 8 balance pass, or wire raw-material sources deeper into the gather/loot flow.

- **2026-05-17** — **Houses & Body Types** — visual banner system. Four archetypal noble houses (Brann/warrior, Aldermere/scholar, Daven/scout, Faldur/cavalier) each with a motto, palette, ordinary (pale/chevron/bend/saltire), charge (swords/book/arrow/horseshoe), and an implicit stat lean (+1 on 3 preferred stats, −1 on 2 discouraged stats — net budget roughly preserved). Four body types (Lean/Burly/Tall/Wiry) roll independently of house — pure visual signal, no stat effect. New data classes `HousePool` and `BodyType`; new Control `BannerIcon` renders procedural heraldry via custom `_draw()` (no PNG assets, scales from 28×36 chip to 132×168 hero). `Unit` gains `house_id` + `body_type` fields, rolled by `RosterGenerator`. Crest appears on every `UnitCard`, large on the Knight Chooser, full hero card on the Knight Overview. Leans are intentionally implicit — motto + origin hint, no stat chips. `SaveManager` extended to persist the new fields and (fixes a pre-existing gap) the chronicle fields `epithet/banner_line/origin_text/oath`; lazy-fills house/body on old saves. GDD §9 expanded with the new section + dated changelog entry. **Next up:** Phase 8 tuning, or layer house-keyed origin pools onto `Chronicle.generate_origin()` so prose clusters by household.

- **2026-05-15** — Resource system foundation. New autoload `ResourceDB` (registered in `project.godot`) defines the full T1–T3 resource tree (18 processed resources + 14 raw materials) with tier→colour mapping and static helpers `best_for_type`, `is_craftable`, `can_afford`. `GameState` gains `gold` (starts at 100), `inventory: Dictionary` (unified raw + processed stockpile), `researched: Array[String]` (research gate stubs), `maintenance_debt: bool`, and `gold_maintenance_cost()` (roster size × 5). `Tick` now deducts weekly gold maintenance, clamping to 0 and flagging `maintenance_debt` if short. Planning screen tabs reordered to **Overview → Tactics → Map → Crafting → Research**; Calendar moved out of the tab bar into a toggle button beside Advance Time. New **Crafting tab** shows raw-material stockpile counts and per-type recipe rows with tier-colour-coded names and Craft buttons (manual, immediate — deducts inputs, adds output). Top-bar resource HUD replaced with a `RichTextLabel` showing Gold + best-held resource per type (Fabric/Timber/Metal) with tier colours (grey T1, green T2, blue T3, purple T4, gold T5). **Next up:** Phase 8 tuning, or wire raw material sources into the gather/expedition flow.

- **2026-05-14** — Phases 5, 6, 7 shipped together. The week is now a real loop end-to-end: Planning → Tick (training, expedition timers/returns, per-training Det-rolled bonus, every-4-weeks Determination) → Pre-Battle Review → Resolution → Battle Log (combat events only) → Weekly Summary → Next Week. New stateless systems: `Tick`, `Combat`, `BattleEvent`, `Resolution` — each one a static helper that takes GameState in and writes results to typed Dictionary buffers (`last_tick_results`, `last_battle_result`). New scenes: `pre_battle_review.tscn` (event-aware setup pane), `battle_log.tscn` (per-unit table), `weekly_summary.tscn` (deltas + returns + rewards + caravan picker + branching Next button), `game_over.tscn`, `run_win.tscn`. Formation editor enforces single-unit-per-slot; live forecast shows player total vs enemy. Every reward formula and enemy-power scaling lives in `Combat.gd` as named static helpers so Phase 8 tuning has one file to touch. **Next up:** Phase 8 — playthrough(s) to tune enemy multipliers, gather base yield, reward sizes; UI polish where the play surfaces it.
- **2026-05-15** — Large UX/systems pass (12 features shipped in one session): **EnemyDB** (9 enemy types with stat ranges + group-power helper); **OutcomeBracket** system (sigmoid win probability, green/orange/red colouring on Pre-Battle forecasts, per-fighter injury rolling on orange/red outcomes — injuries tracked on Unit.injuries, decremented by Tick); **expedition forecast** panel in Map tab (return week + estimated yield per active GATHER); **gold income system** (weekly_stipend + tournament prizes, Tick applies income, cashflow BBCode panel on Overview); **F1 dev toolbar** (debug-only CanvasLayer — inventory add, gold set, advance N weeks, force-queue event, per-unit stat editor with PA cap display); **SaveManager** autoload (full GameState serialisation to user://savegame.json, auto-save on Advance Time, run history persisted to user://run_history.json, save deleted on run end); **stat tooltips** on UnitCard (all 12 stats with hint_tooltip one-liners, injured stats highlighted orange-red); **ConfirmDialogUtil** (modal before Advance, expedition launch, last-ingredient craft — suppress-this-run option stored in GameState.suppressed_confirms); **animated weekly resolution** (sections fade in sequentially via Tween, Next disabled until done); **crafting filter** (recipes hidden when research-locked + no ingredients + never crafted; visible-but-locked shown greyed with lock icon); **weekly diff summary** BBCode RichTextLabel with ▲ arrows, colour-coded training/determination/gold/injury-recovery sections; **main menu rebuild** (left panel: title, seed, New Game, Continue, Options, Quit; right panel: scrollable run history with wins in gold/losses in grey; debug Quick Start button). Also fixed game_over.gd and run_win.gd crash on non-existent GameState.resources field. **Next up:** Phase 8 tuning (balance pass on enemy power, yields, injury rates) and any missing Phase 8 systems.
- **2026-05-14** — Phase 4 complete. `Expedition` class + `active_expedition` on `MapTile` give a clean model for parties in the field. `WorldMapView` is a reusable 15×15 grid widget that the Planning screen drops in; tiles are colour-coded by terrain, Unknown is greyed, castles are red, town is gold, active expeditions show weeks-remaining. The Planning screen wires it all together: event banner, per-unit task picker (Defend / Train *stat*), per-unit checkbox for the next expedition party, separate Explore/Gather launch buttons gated by validation, away-week sub-section with Pillage/Assault and an Explored-castle dropdown, Advance Time button that walks the phase machine (Phase-5/6/7 logic is stubbed for now) and bumps the week. Roster view's Continue button now jumps to Planning. **Next up:** Phase 5 (real Tick — training application, expedition timers/returns, Pre-Battle Review screen with the formation editor).
- **2026-05-14** — Phase 3 complete. Title screen → Knight chooser → Roster view is wired end-to-end. `RosterGenerator` rolls 3 Knight candidates (stats 7–14 +1 flat, PA 100–180) and 3 starting Squires (stats 4–10, PA 60–140) using the seeded RNG, so the whole starting roster is reproducible. `UnitCard.build` is a shared builder used by both chooser and roster; PA stays hidden per GDD §10. `NamePool` provides 32×25 = 800 unique procedural names. `Determination.roll_for_units` honours the PA cap and skips expedition units — Phase 5's Tick will call it on weeks divisible by 4. **Next up:** Phase 4 (TileMap world map + Planning screen for at-home tasks, expeditions, and Away-week choice).
- **2026-05-14** — Phase 2 complete. `GameState` now tracks the run end-to-end (world, roster, resources, tournament streak, current event). `PhaseMachine` (lightweight RefCounted held on GameState) drives the Planning → Tick → Pre-Battle → Resolution cycle and emits `phase_changed` through `EventBus`. `Calendar`, `EventKind`, and `EventRoller` carry the weekly-clock and event-pick logic. Dev scene `scenes/dev/event_roll_test.tscn` (F6) simulates 50 weeks against a pinned seed and validates the tournament + Grand override rules. `main.gd` now prints the phase label so booting the game shows the wiring is live. **Next up:** Phase 3 (Title screen + Knight chooser + Roster view, then the every-4-weeks Determination roll).
- **2026-05-14** — Phase 1 complete. Data classes landed under `scripts/data/`: `Stats`, `ResourceBundle`, `MapTile`, `Castle`, `Unit`, `World`, and the static `WorldGenerator`. `Stats.try_increment` enforces both the 20 cap and the hidden PA cap, so future Train/Determination/Champion's Duel rewards all route through one place. `WorldGenerator.generate(seed)` is deterministic — same seed in, identical world (terrain, knowledge mask, castles, reward bundles) out. Dev scene `scenes/dev/world_dump.tscn` (F6) renders both grids, lists castles, runs all GDD §3/§4 sanity checks, and re-rolls the same seed for a determinism diff. **Next up:** Phase 2 (GameState wiring, weekly phase machine, event roller with tournament override).
- **2026-05-14** — Repo scaffolded (`cd69208`). MVP GDD imported into `GDD.md`. Phase 0 complete: `project.godot`, autoloads (`GameState`, `EventBus`, `RNG`), `Main.tscn`, folder layout.
